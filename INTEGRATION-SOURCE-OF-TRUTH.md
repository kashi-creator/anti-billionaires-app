# Sovereign Society (anti-billionaires-app) — Integration Source of Truth

> **Master reference for any Claude Code session that touches this project.**
> Every session reads this file first. Every session updates the relevant section before exiting. Do not skip — the next session depends on it.

---

## 1. Project Identity

- **Brand name:** Sovereign Society (formerly "The 1% Men's Club" / "Anti Billionaires")
- **Repo:** github.com/kashi-creator/anti-billionaires-app
- **Local repo (laptop):** `~/Desktop/anti-billionaires-app`
- **Local repo (Mac mini):** [TBD — clone path]
- **Hosted:** Railway, project `anti-billionaires-app` (id `57e32801-8f3b-4bd1-bccb-8e1c8d72c3f6`), production environment. Two services: `anti-billionaires-app` (web, gunicorn) + `Postgres`. CLI auth working as `kashi@thebreathcoachschool.com` (manager session 2026-05-02).
- **Live URL:** `https://anti-billionaires-app-production.up.railway.app` (Railway auto-generated, HTTP 200 as of 2026-05-02). The audit-noted legacy URL `https://onepercentmensclub.up.railway.app` (still in old `capacitor.config.json` until commit on this date) is bound to nothing — likely from an older deleted project — and is the **sole** reason the audit reported "production is down." That false alarm is now resolved; production has been quietly serving from the auto-generated URL all along. Capacitor config corrected in same-day commit.
- **Custom domain:** [TBD] — recommended target `app.sovereignsociety.com`, gated on Q7 (sender / brand domain lock). Once locked, attach via `railway domain` and re-point `capacitor.config.json` `server.url` to the custom domain.
- **GHL location ID:** [TBD — Phase 1 fills, separate from Stratum's location]. Not in local env.
- **Stripe account:** existing (separate from Stratum)

---

## 2. Tech Stack

- **Framework:** Flask (Python)
- **DB:** SQLAlchemy + PostgreSQL (Railway) / SQLite (dev)
- **Auth:** Flask-Login + bcrypt
- **Payments:** Stripe (subscription model — already wired)
- **CRM/Automation:** GHL (partially wired — see §6)
- **Templates:** Jinja2 server-rendered HTML
- **File uploads:** Cloudflare R2 bucket `mensgroupapp` via `lib/r2.py` (Phase 9, 2026-05-08); 16MB cap; `static/uploads/` retained as the dev-mode fallback when R2 env unset.
- **Background:** threading (no Celery yet — fine for MVP volume)

---

## 3. App Scope (this is a full social platform — much bigger than Stratum)

Major feature areas, each with its own DB models + routes + templates:

- **Users** — auth, profiles, points, streaks, referrals, location, calendar bookings, preferences
- **Social Feed** — Posts, Comments, Likes, Follow
- **Spaces (Groups)** — SpaceMembership, Space Chat, group polls
- **Polls** — Poll, PollOption, PollVote
- **Events + RSVPs** — three event types (`chapter_recurring`, `weekly_recurring`, `member_meetup`; `official_one_off` reserved). Recurring events live as a "template" row + per-date "occurrence" child rows generated 8 weeks ahead, idempotent on every `/events` GET. Members can post local meetups; chapter / recurrence fields are admin-only (server-side gate). RSVP `going` triggers a confirmation email containing event details + the member's personal `/r/<code>` referral landing URL.
- **Courses** — Course, Lesson, LessonProgress
- **Onboarding Checklists** — ChecklistItem, UserChecklist (slug-based auto-completion; `/manifesto` reading page; 7 canonical items wired into create_post / edit_profile / onboarding / join_space / toggle_follow / event_rsvp / complete_lesson)
- **Direct Messages** — Conversation, Message
- **Stories (IG-style)** — Story, StoryView
- **Wins** — Win, WinReaction
- **Member-to-Member Deals** — Deal, DealInterest
- **Member-to-Member Projects** — Project, ProjectUpdate, ProjectInterest, ProjectPaymentMethod (Phase 7). Member-published builds (businesses/products/missions) with locked vocab on `status`/`project_type`/`visibility`; 3-tier visibility (`members_only`/`brotherhood_only`/`private`); creator-only progress updates; 10-method payment directory (eth/btc/sol/usdc_*/cashapp/venmo/paypal/stripe_link/custom_link) — SS does NOT process, off-platform transactions only.
- **Weekly Challenges** — WeeklyChallenge, ChallengeSubmission, ChallengeVote
- **Resources Library** — Resource, ResourceUpvote
- **Goals + Accountability** — MemberGoal, AccountabilityPair, GoalCheckIn
- **Bookmarks**
- **Gamification** — Badge, UserBadge
- **Reels (video)**
- **AI Chat**
- **Calendar / Bookings** — Availability, CallBooking
- **Activity Feed**
- **Team auto-post queue + every-other-day cron** (Phase 14, 2026-05-11). `TeamPostQueue` model holds future Team posts per Space; `flask cron team-post-publish` (daily @ 13:00 UTC) publishes one queued post per Space whose most recent Team post is older than `TEAM_POST_CADENCE_DAYS` (default 2). Strict FIFO by `(queue_position, created_at)`. Published rows retained for audit (status `pending` → `published`). Manager appends content via `scripts/team_post_enqueue.py` (stdin / `--file` / `--bulk-file`); `/admin/team-queue` surfaces queue state + a per-Space "skip next" control.
- **Notifications**
- **Member Self-Assessment** — 8 Sovereign Code pillars × 5 Likert questions; per-pillar averages stored in `AssessmentResponse` (re-takeable, history preserved); gates the new-signup flow ahead of the 5-step onboarding

User uploads (profile photos, post images, win/deal/story/challenge/event/space/project covers) → Cloudflare R2 bucket `mensgroupapp` (Phase 9, 2026-05-08). DB still stores `uploads/<uuid>.<ext>` paths; `asset_url()` Jinja helper resolves them via 1-hour pre-signed URL when R2 is enabled, otherwise falls back to local-disk via `url_for('static', ...)`. Committed assets (CSS, JS, `img/seed/*`) stay on Railway disk.

Spaces seeded with starter content (Phase 12, 2026-05-09): each of the 8 canonical Spaces holds 15–18 starter posts so a new member visiting any Space sees a populated, lived-in feed instead of an empty room. Off Grid is hand-written verbatim by Kashi (`seed_content/off_grid_posts.txt`, 18 posts); the other 7 (Sovereign Wealth, Body & Iron, Awake Minds, Business Directory, The Arsenal, The Hidden Truth, Family & Legacy) are AI-generated against the locked manifesto voice anchor + the Off Grid posts as the format/density canon. Distribution: round-robin author across active members (`is_admin OR lifetime_access OR subscription_status in ('active','trialing')`, excluding `@sovereign.placeholder` Phase 0B seeds), `created_at` staggered randomly across the past 21 days. Direct ORM inserts only — bypass `app.create_post`'s post-create side effects (no points awards, no checklist auto-checks, no notifications, no GHL pushes). Idempotent on title-prefix match.

---

## 4. Routes Inventory

Verified by Phase 0 audit (2026-05-02). Total: **105 routes** across `app.py` (54) + `phase3_routes.py` (9) + `features_routes.py` (42). Full list — see §13.1 below.

**By area (summary):**
- Public: `/`, `/terms`, `/privacy`, `/pricing`, `/r/<code>` (referral landing)
- Auth: `/login`, `/signup`, `/logout`, `/forgot-password`, `/reset-password/<token>`, `/verify-email/<token>`, `/resend-verification`
- Onboarding: `/onboarding` (GET/POST, 5 steps)
- Feed: `/feed` (GET/POST), `/like/<id>`, `/comment/<id>`, `/post/<id>` (DELETE), `/poll/vote/<id>`
- Profile: `/profile/<id>`, `/profile/edit`, `/members`, `/leaderboard`, `/follow/<id>`, `/profile/location`
- Spaces: `/spaces`, `/space/<id>`, `/space/create`, `/space/<id>/{join,leave,post}`, `/space/<id>/chat`, `/space/<id>/chat/{send,poll}`
- DMs: `/messages`, `/messages/new/<user_id>`, `/messages/<convo_id>`, `/messages/<convo_id>/{send,poll}`, `/api/messages/unread-count`
- Stories: `/stories/create`, `/stories/<id>`, `/api/stories`
- Wins: `/wins`, `/wins/create`, `/wins/<id>/react`
- Deals: `/deals`, `/deals/create`, `/deals/<id>`, `/deals/<id>/interest`
- Resources: `/resources`, `/resources/create`, `/resources/<id>/upvote`
- Challenges: `/challenges`, `/challenges/create`, `/challenges/<id>`, `/challenges/<id>/submit`, `/challenges/submission/<id>/vote`
- Events: `/events`, `/events/<id>`, `/events/create`, `/events/<id>/rsvp`
- Lessons (Vault): `/learn` (alias), `/lessons`, `/lessons/<course>/<lesson>`, `/lessons/<course>/<lesson>/complete`
- Welcome checklist: `/welcome`, `/welcome/check/<id>`
- Goals / Accountability: `/accountability`, `/accountability/pair/<user>`, `/accountability/goals/{create,<id>/checkin,<id>/complete}`
- Bookmarks: `/bookmarks`, `/bookmark/<post_id>`
- Bookings: `/book/<user>`, `/book/<user>/create`, `/bookings`, `/bookings/<id>/{confirm,cancel}`
- Reels: `/reels`, `/reels/create`
- Wingman (AI): `/wingman`, `/wingman/send`
- Map: `/map`
- Boardroom: `/boardroom`
- Misc: `/badges`, `/spotlights`, `/activity`, `/search`, `/referrals`, `/preferences/digest`
- Notifications: `/notifications`, `/notifications/read`, `/notifications/mark-read`, `/api/notifications/unread-count`, `/api/notifications/recent`
- Native (Capacitor): `/api/devices/{register,unregister}`
- Pricing/Billing: `/pricing`, `/validate-code`, `/create-checkout-session`, `/subscription/success`, `/billing-portal`, `/webhook/stripe`
- Admin: `/admin`, `/admin/member/<id>`, `/admin/{toggle-admin,toggle-subscription,grant-lifetime,revoke-lifetime,refund-last,comp-month}/<id>`

**No duplicate routes detected.** Two near-duplicates that are intentional: `/notifications/read` and `/notifications/mark-read` (both POST, both mark-all-read; the second is a leftover; safe to leave but flag for cleanup). The `/learn` route is a 302 redirect alias to `phase3.lessons`.

**Stub / partial routes flagged:**
- `/wingman/send` (`features_routes.py:894`) returns a placeholder reply if `ANTHROPIC_API_KEY` is missing or contains "placeholder"/"replace" — works, but unconfigured in local env. Acceptable for MVP.
- `/create-checkout-session` (`app.py:1237`) returns a 400 with "Payment is not configured yet. Use a founder code to join." if `STRIPE_SECRET_KEY` looks placeholder. Defaults to founder-code path.

---

## 5. Environment Variables

### Currently expected by code (verified by Phase 0 audit, grep across all py files)

| Var | File:line | Default | Required in prod |
|-----|-----------|---------|------------------|
| `FLASK_ENV` | `app.py:40` | `development` | Yes (set to `production`) |
| `SECRET_KEY` | `app.py:41` | none — raises in prod | **Yes — hard requirement** |
| `DATABASE_URL` | `app.py:49` | `sqlite:///abmc.db` | **Yes** (Railway Postgres) |
| `STRIPE_SECRET_KEY` | `app.py:82` | `sk_test_placeholder` | **Yes** (live or test) |
| `STRIPE_PUBLISHABLE_KEY` | `app.py:83` | `pk_test_placeholder` | **Yes** |
| `STRIPE_WEBHOOK_SECRET` | `app.py:84` | `whsec_placeholder` | **Yes** |
| `STRIPE_PRICE_ID` | `app.py:85` | `price_placeholder` | **Yes** |
| `GHL_API_KEY` | `app.py:89` | empty (skip GHL) | Yes (no-op silently if unset) |
| `GHL_LOCATION_ID` | `app.py:90` | empty | Yes (paired with above) |
| `ADMIN_EMAILS` | `app.py:180` | empty (skipped) | Optional (defense-in-depth allowlist) |
| `FOUNDER_CODES` / `FOUNDER_CODE` | `app.py:1231` | `ABMC2026` | Optional (founder bypass codes) |
| `WINGMAN_DAILY_MESSAGE_CAP` | `features_routes.py:873` | `50` | Optional |
| `ANTHROPIC_API_KEY` | `features_routes.py:912` | empty (placeholder mode) | Optional (AI Wingman) |
| `ANTHROPIC_MODEL` | `features_routes.py:919` | `claude-sonnet-4-6` | Optional |
| `RESEND_API_KEY` | `email_send.py:33` | empty (console stub) | **Yes** (transactional email) |
| `EMAIL_FROM` | `email_send.py:21` | `onboarding@resend.dev` | **Yes** (custom sender domain) |
| `EMAIL_FROM_NAME` | `email_send.py:22` | `Sovereign Society` | Optional |
| `PORT` | `app.py:1709` (post-fix) | `5000` | Optional (Railway sets it) |
| `R2_ACCESS_KEY_ID` | `lib/r2.py` | empty (disable R2, fall back to disk) | **Yes** for prod (Phase 9) |
| `R2_SECRET_ACCESS_KEY` | `lib/r2.py` | empty | **Yes** for prod (Phase 9) |
| `R2_ENDPOINT` | `lib/r2.py` | empty | **Yes** for prod — Cloudflare R2 endpoint URL |
| `R2_BUCKET` | `lib/r2.py` | empty | **Yes** for prod — `mensgroupapp` |
| `TEAM_POST_CADENCE_DAYS` | `cron.py` (Phase 14) | `2` | Optional — days between Team auto-posts per Space. Set 7 for weekly, etc. |

### Phase 0 verification (2026-05-02)
- **Local shell**: ALL of the above are UNSET in this dev environment. `python app.py` runs with the dev SECRET_KEY warning + sqlite + placeholder Stripe + empty GHL. Acceptable for local; nothing to leak.
- **Railway dashboard**: Railway CLI is installed but not linked from this machine (`railway variables` → "No linked project found"). Manual verification via Railway dashboard required to confirm production env. Phase 1 should run `railway link` once and document.

### Phase 1 additions (consumed by `lib/ghl.py`; all optional, client no-ops if unset)
- `SERVER_NAME` — canonical host for `url_for(_external=True)` outside request context (set in Railway to `anti-billionaires-app-production.up.railway.app` until custom domain locks). Threaded email render needs this.
- `GHL_PIPELINE_ID` — the `Sovereign Society — Member Lifecycle` pipeline id (created in GHL UI by Kashi after Phase 1)
- `GHL_STAGE_PROSPECT_ID`
- `GHL_STAGE_TRIALING_ID`
- `GHL_STAGE_ACTIVE_ID` (active-member)
- `GHL_STAGE_POWER_ID` (power-member)
- `GHL_STAGE_LIFETIME_ID` (lifetime-qualified)
- `GHL_STAGE_AT_RISK_ID`
- `GHL_STAGE_CANCELLED_ID`

### Likely needed in later phases
- `GHL_ONBOARDING_WORKFLOW_ID`
- `GHL_CANCELLATION_WORKFLOW_ID`

---

## 6. GHL Integration — Current State

**Phase 1 (2026-05-02) lifted the inline `ghl_upsert_contact()` to `lib/ghl.py` and locked the canonical tag taxonomy.** The previous "What IS missing" list below now reflects post-Phase-1 state.

### `lib/ghl.py` shape (locked Phase 1)

Single canonical entry point. Public surface:
- `ghl.upsert_contact(*, email, name, phone=None, stage_tag=None, custom_fields=None, extra_tags=None)` — POSTs to `https://services.leadconnectorhq.com/contacts/upsert` with header `Version: 2021-07-28`. Stage tags only (`STAGE_TAGS` set). `ValueError` on any non-canonical tag and on stage tags misrouted into `extra_tags`. Daemon-thread send, fail-silent. No-ops when `GHL_API_KEY`/`GHL_LOCATION_ID` unset.
- `ghl.upsert_opportunity(*, contact_email, stage_tag, monetary_value=99.0)` — Phase 2 stub. Reads `GHL_PIPELINE_ID` + `GHL_STAGE_<NAME>_ID`; logs and returns when env unset (no network call yet).
- `ghl.custom_fields_from_user(user) -> dict` — builds the standard 4-field dict (`payments_made_count`, `qualified_referrals_count`, `lifetime_access` as `"true"`/`"false"`, `lifetime_qualified_at` as ISO date or empty string).
- `ghl.STAGE_TAGS` — frozen set of the 9 canonical stage tags.

Payload JSON:
```json
{
  "email": "<lower>",
  "name": "<display name>",
  "locationId": "<GHL_LOCATION_ID>",
  "tags": ["<one stage tag>", "<extra...>"],
  "phone": "<E.164>",
  "customField": [
    {"key": "payments_made_count", "field_value": "..."},
    {"key": "qualified_referrals_count", "field_value": "..."},
    {"key": "lifetime_access", "field_value": "true|false"},
    {"key": "lifetime_qualified_at", "field_value": "YYYY-MM-DD"}
  ]
}
```

### Canonical Tag Taxonomy (locked Phase 1)

Stage tags only. **Brand tags forbidden** (the GHL location boundary is the brand boundary). A contact carries exactly ONE current-stage tag; the upsert API replaces tags atomically when `tags=` is passed, so old stage tags are swept on each transition.

| Stage | Canonical tag |
|-------|---------------|
| 1 — Prospect | `prospect` |
| 2 — Trialing | `trialing` |
| 3 — Active Member | `active-member` |
| 4 — Power Member | `power-member` |
| 5 — Lifetime-Qualified | `lifetime-qualified` |
| 6 — At-Risk | `at-risk` |
| 7a — Trial-Cancelled | `trial-cancelled` |
| 7b — Member-Cancelled | `cancelled` |
| 8 — Reactivated | `reactivated` |

### Call sites + tags currently applied (post-Phase 1, verified 2026-05-02)

All call sites also pass `custom_fields=ghl.custom_fields_from_user(u)` except `/signup` (free path, no User row yet beyond name/email).

| File trigger | Stage tag | Notes |
|---|---|---|
| `/signup` POST (free signup, no card) | `prospect` | Pure prospect — has account but no payment method. |
| `/signup-with-code` POST (founder-code path) | `lifetime-qualified` | Account is created `lifetime_access=True`. |
| `/subscription/success` POST after Stripe checkout creates account | `trialing` | 30-day trial on file. |
| `customer.subscription.deleted` webhook | `trial-cancelled` (if `payments_made_count == 0`) or `cancelled` | Different win-back audiences. |
| `invoice.payment_succeeded` webhook (every successful charge) | `active-member` | NEW in Phase 1 — keeps custom fields fresh on every payment. |
| `invoice.payment_succeeded` → referrer hits lifetime threshold | `lifetime-qualified` | Referrer's tag flip (paying user gets `active-member` in same handler). |

### What IS still missing (Phase 1 → Phase 2 boundary)

- Webhook coverage gaps remain on `checkout.session.completed`, `customer.subscription.updated`, `invoice.payment_failed` — **Phase 2 scope.**
- Engagement-driven tag transitions (`power-member` on activity threshold, `at-risk` on inactivity, `reactivated` on return) — **Phase 4 scope.**
- No live GHL workflows yet (Kashi sets up in GHL UI separately).
- Pipeline / stage IDs (`GHL_PIPELINE_ID`, `GHL_STAGE_*_ID`) not yet set in Railway env. `ghl.upsert_opportunity()` no-ops until they are.
- `lib/ghl.py` does NOT remove old stage tags explicitly — it relies on GHL's atomic-replace-on-upsert behavior. If GHL's API semantics change, transitions will leak old tags. (Phase 2 should verify against the live GHL location once creds are in.)
- Backfill: `scripts/backfill_ghl_tags.py` is shipped (dry-run by default) but **not yet run against the live GHL location**. Kashi runs `--apply` after reviewing the dry-run output once Bryce shares the location creds.

---

## 7. Customer Journey Stages (membership-flavored, NOT same as Stratum)

| Stage | Tag entry | Description | Channel |
|-------|-----------|-------------|---------|
| 0 — Visitor | (anonymous) | Hits landing page | Pixel + retargeting |
| 1 — Prospect | `prospect` | Email-only lead (lead magnet, referral landing) — no account, no card | Email nurture |
| 2 — Trialing | `trialing` | Signed up with card on file, in 30-day free trial (Stripe status `trialing`) | Onboarding + activation push, day-28 conversion reminder |
| 3 — Active Member | `active-member` + tier-specific | First real charge landed (day 31+, Stripe status `active`) | Engagement loops |
| 4 — Power Member | `power-member` | High activity (posts, course completion, events) | VIP perks, referral push |
| 5 — Lifetime-Qualified | `lifetime-qualified` | Earned lifetime access (3 referrals × 6 paid months each) — billing stopped | Recognition + retention reinforcement |
| 6 — At-Risk | `at-risk` | Low activity 21+ days | Re-engagement |
| 7a — Trial-Cancelled | `trial-cancelled` | Cancelled during 30-day trial, never charged | Different win-back: re-trial offer or testimonial-led re-engagement |
| 7b — Member-Cancelled | `cancelled` | Was paying, cancelled subscription | Standard 3-email win-back over 30 days |
| 8 — Reactivated | `reactivated` | Came back after any cancel | Re-onboarding |

---

## 8. Phase Status

| Phase | Status | Notes |
|-------|--------|-------|
| 0 — Current state audit | ✅ done | Routes, env vars, Stripe wiring, GHL wiring, deploy/email/DB state documented in §13. Two 1-line bug fixes shipped in commit `1116d92` (port 5000 hardcode → `PORT` env; `has_active_subscription` accepts `"trialing"`). Audit report committed separately. Surfaced new launch-blockers: production currently 404s, `cron.py` digest references nonexistent fields, brand-tag inconsistency in GHL calls. |
| 0A — Public copy + Terms alignment with locked business model | ✅ done | Rewrite landing.html FAQ #2/#5/#6, fix pricing.html ($100 → $99, add trial + lifetime mechanic), rewrite legal.html Terms §2 with proper subscription disclosure. Prompt at `phase-prompts/phase-0a-copy-terms-alignment.md` — completed 2026-05-02, commit e4aa7fc |
| 0B — Seed empty community with placeholder content + imagery | ✅ done | 8 Founding-Voice users, 16 posts (2 per canonical Space), 4 wins, 4 deals, 7 resources (+14 upvotes), 1 active challenge (+3 submissions), 14 RSVPs across the 3 canonical events. 13 Nano Banana images + 8 SVG monogram avatars. Legacy 6 Spaces + 5 Events purged. Idempotent re-run + `--delete` flag. Completed 2026-05-02, commit 78f8f47. `_seed_content()` idempotency was already correct — no `app.py` change needed. |
| 0C — Wire founder-code onboarding (server side) | ✅ done | New `POST /signup-with-code` route creates a fully-active lifetime account from a valid founder code (lifetime_access=True, subscription_status=active, email_verified=True, lifetime_qualified_at set, is_admin=False). `_is_founder_code()` helper extracted in `app.py`; `/validate-code` refactored onto it. `templates/pricing.html` JS now branches the main CTA on `dataset.founder` — code-applied path swaps the email field for a 3-input mini-form (name/email/password) and submits to `/signup-with-code`; Stripe path unchanged. CSRF meta tag added to `templates/base.html` head; `require_csrf` decorator extended to also accept the `X-CSRFToken` header so JSON fetch callers can pass the token without form-encoding. GHL push uses legacy tags `["Lifetime", "ABMC"]` — Phase 1 will sweep to canonical taxonomy. `send_lifetime_unlocked` called with graceful degrade (try/except + RESEND console fallback). Smoke-tested locally on Flask 3.0 / port 5055/5056: pricing.html serves CSRF meta, validate-code valid+invalid, signup-with-code validates required fields / 10-char password / valid code / duplicate email; success path returns `{"redirect": "/welcome"}` and DB user has all four field flips correct; CSRF header enforcement works (missing header → 400); rate-limit `5/min` confirmed; logged-in lifetime user gets HTTP 200 on `/feed` (no paywall); `/billing-portal` 302-redirects to `/pricing` for lifetime account (no Stripe customer) without crashing; email stub printed to stdout. Completed 2026-05-02. |
| 1 — Lift GHL into proper client + tighten existing integration | ✅ done | `lib/ghl.py` shipped (commit `99cc51b`); `cron.py` field bugs fixed (commit `e5ffdaf`); `scripts/backfill_ghl_tags.py` + SoT updated. 9-tag canonical taxonomy locked; 5 call sites migrated; 4 custom fields sync; SERVER_NAME / PREFERRED_URL_SCHEME wired. Backfill script NOT executed against live — Kashi runs `--apply` separately. Completed 2026-05-02. |
| 2 — Stripe → GHL webhook flow | ⬜ pending | On subscription created/updated/cancelled, mirror to GHL contact + opportunity in Member pipeline |
| 3 — Member intake / first-day onboarding journey in GHL | ⬜ pending | Welcome email, profile completion prompts, content discovery, first-7-days nurture |
| 4 — Engagement automations | ⬜ pending | Tag application on post created, course completed, win posted, event RSVP — drives nurture and gamification |
| 5 — Cancellation / win-back | ⬜ pending | Trigger on subscription cancelled, 3-email win-back sequence over 30 days |
| 6 — Customer Journey Playbook (community context) | ⬜ pending | Membership-flavored playbook (not e-commerce like Stratum) |
| 7 — Compliance + community guidelines | ⬜ pending | Content policy, terms, COPPA if any minors, payment compliance |
| 3 — Events stack rework: chapters, recurring, member meetups (app build) | ✅ done | New schema fields on `Event` (`event_type`, `chapter`, `recurrence_rule`, `recurrence_parent_id`, `is_recurrence_template`) shipped in migration `b1e2f5a6c7d8`; locked vocabularies enforced via `@validates` on the model (`EVENT_TYPES`, `RECURRENCE_RULES` frozen sets in `models.py`). Recurrence model: each calendar occurrence is its own `Event` row pointing back at a `is_recurrence_template=True` parent (preserves the existing `EventRSVP.event_id` join — no ephemeral occurrences). New `_generate_upcoming_occurrences(template, weeks_ahead=8)` helper in `app.py` materializes the next 8 weeks idempotently (existence-checked by `(recurrence_parent_id, date)`); called from `_seed_content()` and from `/events` GET on every request. The 3 stale Phase-0B seeds (`Fire to Fire - St. Pete`, `Sovereign Wealth Workshop`, `Brotherhood Summit`) are deleted on every boot. New seed: `St. Petersburg Chapter Biweekly` (chapter_recurring, `first_and_last_thursday_monthly`, 6:30 PM EST @ The Temple, 155 8th Street North, Saint Petersburg, FL 33701) + `Thursday Group Lunch` (weekly_recurring, `every_thursday`, time/location blank — admins fill per occurrence). `phase3_routes.events` route groups by `chapter_recurring` (sectioned by `chapter`), `weekly_recurring`, `member_meetup` (with empty-state CTA "Post a Meetup"). `phase3_routes.create_event` opens to any `has_active_subscription` member via local `_paywall_required` decorator (mirrors `app.paywall_required` to avoid the existing `app → phase3_routes` circular import); non-admins are server-side-forced to `event_type=member_meetup`, `recurrence_rule=none`, `is_recurrence_template=False`. Admins additionally see `event_type` and `recurrence_rule` selects in the form. `event_detail.html` shows a "Part of: <template title> (recurring)" line above the date when `recurrence_parent_id` is set, plus a chapter line. Smoke-tested on a fresh sqlite DB: 14 events seed (2 templates + 4 St. Pete biweekly occurrences + 8 Thursday lunch occurrences), idempotent re-seed (count stays 14), three sections render with no stale events, admin Austin chapter create produces 4 occurrences, member bypass (POSTing `event_type=chapter_recurring`) is forced to `member_meetup`, RSVP attaches to the occurrence (`event_id=7`) NOT the template (id=2), `Event(event_type='not_a_real_type')` and `Event(recurrence_rule='monthly_first')` both raise `ValueError`. NOT in scope: `.ics` calendar invites, RSVP confirmation emails, GHL engagement-tagging on RSVP — all Phase 4. Completed 2026-05-03. |
| 5 — Member Self-Assessment (app build) | ✅ done | New `AssessmentResponse` model + `user.assessment_complete` column (migration `f5a6b7c8d9ec`); 8 pillars × 5 verbatim Likert questions in `lib/assessment.py`; 4 routes (`/assessment` GET, `/assessment/submit` POST JSON, `/assessment/results` GET, `/assessment/skip` POST); `_post_signup_redirect` + `_post_signup_redirect_url` helpers chain `assessment → onboarding → feed`; both `/subscription/success` (existing-user + new-user branches) and `/signup-with-code` rewired to use the helpers. Single-page wizard template (8-pillar JS pagination, dot progress, force-pick Likert, no defaults) + results template (8 plain `4.2 / 5` rows, "Begin onboarding" CTA). Skip = flips `assessment_complete=True` without creating a row. Re-take = appends a new row, never overwrites. Smoke tests: GET routes 200; valid submit returns scores + redirect; missing pillar 400 with offending slug; out-of-range value 400 with `pillar`+`question_index`; CSRF enforced; results page reads latest row; re-take creates row #2 leaving row #1 intact. Score math sanity verified ([1..5] avg=3.0, [5,5,5,5,5]=5.0, [1,1,1,1,1]=1.0, [3,3,3,3,3]=3.0). NOT using `@paywall_required` on assessment routes — that decorator force-redirects to `/onboarding` when onboarding is incomplete, which would loop assessment users past the assessment; instead each route does `@login_required` + manual `has_active_subscription` check (mirrors `/onboarding`'s pattern). Completed 2026-05-03. |
| Welcome checklist wiring + manifesto page (post-launch UX, parallel to GHL Phase 4) | ✅ done | New `ChecklistItem.slug` column (migration `f5a6b7c8d9ea`, unique nullable). 7 canonical items locked: complete-profile, read-manifesto, first-post, join-space, follow-brothers (relaxed threshold from 5→3), rsvp-event (decoupled from the wiped Fire to Fire event), complete-lesson. New `_check_item_by_slug(user_id, slug)` replaces fragile `_auto_check_item(user_id, title_substring)` (kept as a thin shim for safety). Auto-check sites wired in `app.py:create_post`, `app.py:edit_profile`, `app.py:onboarding_submit`, `app.py:join_space`, `app.py:toggle_follow`, `phase3_routes.event_rsvp`, `phase3_routes.complete_lesson`, and on GET of `/manifesto`. New `/manifesto` route + `templates/manifesto.html` lifts manifesto copy verbatim from `templates/landing.html` lines 575-579 ("engineered reality" → "build the fire"); centered 720px reading column with serif type, gold-on-black, single "Back to the Society" CTA. `seed_checklist()` rewritten with self-heal: legacy "Introduce Yourself" → first-post, "Join 2 Spaces" → join-space, "Follow 5 Brothers" → follow-brothers slugs are backfilled by alias-title match (preserving existing user completions); legacy "RSVP to Fire to Fire" row + its UserChecklist children are wiped wholesale (event no longer exists post Phase 3 UX, completion not transferable); any orphaned slugless row left over is also swept. Sidebar widget on `/feed` capped at 5 items (incomplete first by `order_index`) with a "See all 7 steps" overflow link. `/feed?focus=composer` query param auto-focuses the post composer textarea on load. Smoke-tested on the local Flask test client: fresh DB seeds 7 items with non-null slugs in correct order; legacy-DB simulation with 5 stale rows converges to canonical 7 with the legacy "Introduce Yourself" completion preserved under the renamed first-post slug; `/manifesto` 200 with copy intact + `Back to the Society` button + auto-check fires; `/feed?focus=composer` 200 with focus JS injected; sidebar shows exactly 5 entries + "See all 7 steps" overflow text; `_check_item_by_slug` is idempotent (calling twice on same slug yields one UserChecklist row, no duplicate commits). Completed 2026-05-03. |
| 7 — Member-published projects (app build) | ✅ done | Four new tables shipped via migration `c1d2e3f4a5b6` (chains off Phase 6 `f5a6b7c8d9e0`): `Project` (user-owned, locked `status`/`project_type`/`visibility` vocabs enforced via `@validates`), `ProjectUpdate` (creator-only progress notes), `ProjectInterest` (idempotent toggle, unique constraint on `(project_id, user_id)`), `ProjectPaymentMethod` (10 locked types: eth, btc, sol, usdc_eth, usdc_sol, cashapp, venmo, paypal, stripe_link, custom_link — each with anchored full-match shape regex; **SS does NOT process — directory only, off-platform transaction**). 9 new routes on the `features` blueprint: `/projects` (filterable feed), `/projects/<id>` (detail), `/projects/create`, `/projects/<id>/edit`, `/projects/<id>/interest` (toggle), `/projects/<id>/update` (creator-only), `/projects/<id>/archive` (soft-delete, creator-only), `/projects/<id>/payment-method` (add, creator-only), `/projects/<id>/payment-method/<pm_id>/delete` (remove, creator-only). Visibility helper `_visible_projects(query, user)`: `lifetime_access` sees `members_only` + `brotherhood_only`; `has_active_subscription` (no lifetime) sees `members_only` only; `private` is creator-only and never returned by the helper. Self-view exception in detail/edit routes — creator always sees own active projects regardless of tier. Paywall + onboarding gate inlined per route via `_project_paywall_or_redirect()` (mirrors `app.paywall_required` to dodge the `app → features_routes` import cycle, same pattern as Phase 6). Profile-page integration: `/profile/<id>` now passes `projects` (active, visibility-filtered) to `templates/profile.html` which renders a Projects section before Posts. Templates: `templates/projects.html` (feed with filter pills), `templates/project_detail.html` (load-bearing payment-disclaimer banner: "Sovereign Society does not process these payments. You're transacting directly with the project creator off-platform. We do not custody, escrow, or refund." — copy must stay verbatim or close paraphrase to keep SS out of money-transmitter scope), `templates/create_project.html`, `templates/edit_project.html` (with archive danger-zone). "Projects" entry added to the avatar dropdown (between My Deals and Referrals) and the mobile menu (between Deals and Challenges). Soft cap of 10 payment methods per project. Smoke tests passed locally on Flask test client (per-user fresh test clients to avoid SQLAlchemy session staleness): validation rejects invalid `status`/`project_type`/`visibility`/`method_type` with `ValueError`; Bob (members_only sub) sees only `members_only` projects on feed and gets 404 on `brotherhood_only` detail; Cody (Lifetime) sees both tiers; interest toggle is idempotent (re-click removes); non-creator gets 403 on `/update`/`/archive`/`/payment-method/*`; creator update succeeds and bumps `project.updated_at`; ETH valid shape (0x + 40 hex) accepted, bogus shape rejected without DB write; cashapp `$tag` accepted; archive flips `is_active=False`, hides project from public feed (and 404s detail for non-creator), creator still reaches own archived project via direct URL with banner; profile integration gates by tier (Bob sees no projects when both Alice projects are archived/brotherhood-only; Cody-lifetime sees brotherhood-only on Alice profile; Alice on own profile sees active only, archived hidden). 0 `ghl.` references in `features_routes.py` (no engagement-tagging on project create — Phase 8 territory). NOT in scope: notifications ("X expressed interest in your project"), member discussion threads on `ProjectUpdate`, GHL engagement-tagging, project-as-Deal-category, `private` visibility UI, no checklist 8th item (Phase 4's 7-item list is final). Completed 2026-05-03. |
| 6 — Member location search + proximity detection (app build) | ✅ done | Three-tier privacy model on `User.location_visibility` (`hidden` / `city_only` / `proximity_visible`, default `city_only`) — migration `f5a6b7c8d9e0` adds the column and backfills `'hidden'` for legacy `show_on_map=False` users. `lib/geocoding.py` wraps Nominatim (keyless, OSM-backed, ~1 req/sec polite-use cap) with `functools.lru_cache(maxsize=1024)` and ships a `haversine_miles` helper. Three new routes on the `features` blueprint: `GET /find` (privacy-aware tabbed UI — "Who's in your city" + "Brothers in your area"), `POST /find/search` (geocodes the query, filters `User.location_visibility != 'hidden'` within the chosen 25/50/100 mi radius, returns a sorted JSON list with city/country/miles/photo — never raw lat/lng), `POST /find/nearby` (browser geolocation → 20 closest `proximity_visible` members). Paywall + onboarding gates inlined in each route to avoid the `app → features_routes` circular import. New `POST /profile/visibility` endpoint reads the 3-radio set added to `templates/edit_profile.html`; the same radio set is added to `templates/onboarding.html` step 3 below the country input (default `city_only`, processed by `app.py:onboarding_submit` step-3 branch). `show_on_map` is kept in sync (`!= 'hidden'`) for backwards-compat with the existing `/map` Leaflet view. "Find Brothers" link added to the avatar dropdown + mobile menu in `templates/base.html`. Smoke-tested locally on Flask 3.0 / port 5099 against a 4-user Austin fixture (proxA + proxB at `proximity_visible`, cityonly at `city_only`, hidden at `hidden`): Austin city search returns the 3 visible users (hidden excluded); `/find/nearby` from Austin coords returns only the 2 `proximity_visible` users (city_only excluded); flipping proxA to `hidden` removes it from the city search; garbage city `"ksjdhfksjd"` returns 400 with `"could not locate that city"`; lru_cache cuts the second `Boise, ID` lookup from ~0.55s to ~0.005s; no regressions on `/map`, `/members`, `/profile/edit`, `/feed`; `POST /profile/visibility` flips the column and 302-redirects to `/profile/edit`. NOT in scope: ripping out `/map` or `templates/member_map.html` (kept as-is), automatic geocoding on profile save, native iOS geolocation plugin (browser API works inside the Capacitor WebView). Completed 2026-05-03. |
| 9 — Migrate uploads to Cloudflare R2 | ✅ done | `lib/r2.py` boto3 wrapper (4 helpers: `upload_fileobj`, `presigned_url`, `head_object`, `delete_object`; cached `_client()`; `enabled()` short-circuits to False if any of `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` / `R2_ENDPOINT` / `R2_BUCKET` is unset — local dev keeps working without R2 creds). `save_upload()` in `app.py` rewrites streams to R2 first; on R2 failure falls back to disk so the user's file isn't dropped. Same refactor in `features_routes._save_upload` and `phase3_routes._save_upload` (both blueprints had their own copy of the helper). New `asset_url(path)` Jinja context-processor (`app.py`) — routes `uploads/...` paths through 1-hour pre-signed URLs when R2 enabled, leaves committed assets (`img/seed/...`, `css/`, `js/`) on the normal `url_for('static', ...)` flow. Templates audited and swapped at every site rendering a user-uploaded model field (`User.profile_photo`, `Post.image_path`, `Story.image_path`, `Win.image_path`, `Deal.image_path`, `Reel.author.profile_photo`, `ChallengeSubmission.image_path`, `Event.cover_image`, `Space.cover_image`, `Project.cover_image`, `ProjectUpdate.image_path`) — 28 sites across 26 templates: `base.html`, `feed.html`, `profile.html`, `space_detail.html`, `spaces.html`, `messages_inbox.html`, `messages_chat.html`, `wins.html`, `deals.html`, `deal_detail.html`, `challenge_detail.html`, `bookmarks.html`, `boardroom.html`, `event_detail.html`, `events.html`, `lessons.html`, `members.html`, `leaderboard.html`, `spotlights.html`, `search.html`, `reels.html`, `story_view.html`, `referrals.html`, `accountability.html`, `onboarding.html`, `edit_profile.html`, `projects.html`, `project_detail.html`, `edit_project.html`, `booking.html`. `Reel.thumbnail_path` field exists but no template renders it — no swap needed. Static assets unchanged. New `scripts/null_broken_uploads.py` (one-shot, idempotent) sweeps `User.profile_photo` and nulls any value whose underlying file is gone in both R2 and local disk — manager runs post-deploy via `railway run` to clean the 5 users with broken paths from Railway's ephemeral-disk era. R2 bucket `mensgroupapp` already exists on Bryce's Cloudflare account; 4 R2 env vars set on Railway by manager 2026-05-08. boto3 round-trip verified (write+head+presigned+delete all True against the live bucket). Smoke tests: `enabled()` False with no env / True with env, R2 round-trip 4/4 ops succeed, local-disk fallback writes file under `static/uploads/` when R2 env unset. NOT in scope: custom domain (`media.sovereignsociety.rich`) — gated on Bryce DNS switch; image processing/thumbnails; migrating committed seed images. Completed 2026-05-08. |
| 10 — Form-submit UX polish (post composer + profile photo) | ✅ done | `templates/feed.html` `postForm` submit handler disables the submit button + sets `Posting...` state, clears textarea / image preview / poll-creator on submit (50ms `setTimeout` lets native form serialization capture values before the wipe). Prevents the duplicate-post bug (Kashi posted the same content 3× on 2026-05-09 because the button stayed clickable through the redirect). `templates/edit_profile.html` adds an `onchange` handler on `#profile_photo` that swaps the `.avatar.avatar-xl` inner HTML to a fresh `<img>` whose src is a `URL.createObjectURL` blob URL — instant preview before Save (covers both the photo-set and initial-letter variants). Form still posts natively; no `preventDefault`. No server changes; R2 upload pipeline (Phase 9) unaffected. Completed 2026-05-09. |
| 12 — Seed all 8 Spaces with starter content | ✅ done | New `scripts/seed_space_content.py` (two modes: `generate` calls Anthropic + writes per-Space files under `seed_content/`; `insert` reads files + writes directly via SQLAlchemy). 7 AI-generated Spaces (Sovereign Wealth, Body & Iron, Awake Minds, Business Directory, The Arsenal, The Hidden Truth, Family & Legacy) at 15 posts each + 1 hand-written Space (Off Grid, 18 posts written verbatim by Kashi at `seed_content/off_grid_posts.txt`). Generation uses `claude-sonnet-4-6` with prompt caching on the shared voice anchor (manifesto block from `templates/landing.html` lines 589-593 + 3 Off Grid posts as format canon + format rules). Per-Space theme briefs lock domain. Validators reject em-dashes, exclamation points, banned phrases (`delve into`, `journey of`, `unleash`, `tapestry`, `realm`, etc.), and word counts outside 80–400; up to 2 retries per Space. Round-robin attribution across active members (`is_admin OR lifetime_access OR subscription_status in ('active','trialing')`, filtering out `@sovereign.placeholder` Phase 0B seeds). 21-day `created_at` stagger. Idempotent on title-prefix match. **Bypasses `app.create_post` side effects** — direct ORM inserts skip the points award, the `_check_item_by_slug("first-post")` checklist auto-check, all notifications, all GHL pushes — so members do NOT get notification spam when this script runs. Local smoke tests passed: live generation against `Sovereign Wealth` produced 15 posts (130–180 words each) all passing validation on first attempt; insert into local SQLite distributed evenly across 3 active users (5/5/5) with timestamps spread across 10 distinct days; idempotent re-run skipped all 15; Off Grid insert works verbatim from the file (18 posts inserted, bullet character `•` preserved). NOT in scope: spaces' DB descriptions or cover images (untouched); modifying `_seed_content()` (untouched); changing the post create route or its side effects; AI-generating Off Grid (Kashi-written stays Kashi-written); poll/image/special-format posts (plain `Post.content` only). Completed 2026-05-09. |
| 13 — Events cleanup (lock 6 canonical biweekly dates) + RSVP confirmation email | ✅ done | Two coupled fixes. (1) Phase 3's `first_and_last_thursday_monthly` recurrence rule didn't match the actual cadence Kashi locked (every-other-Thursday biweekly anchored to May 14, NOT 1st-and-last-monthly). Added `manual` to `RECURRENCE_RULES` vocab in `models.py`; `_generate_upcoming_occurrences` early-returns on `manual`; `_seed_content` self-heals the existing template's `recurrence_rule` from `first_and_last_thursday_monthly` → `manual`, refreshes description/time/location, wipes child Events whose dates aren't in the canonical 6-date set (May 14, May 28, Jun 11, Jun 25, Jul 9, Jul 23), and idempotently inserts the canonical 6 occurrences (self-heals time/location on re-run). EventRSVP rows for stale children are deleted to satisfy the FK before the parent row drops. (2) New `email_send.send_event_rsvp_confirmation(user, event, referral_url)` + `templates/emails/event_rsvp_confirmation.html` + `.txt` (gold-on-black, manifesto voice, no em-dashes/exclamation points). Wired into `phase3_routes.event_rsvp` after the `_check_item_by_slug("rsvp-event")` call: on `status=='going'` only (skipped for `interested`/`not_going`), calls `current_user.ensure_referral_code()` + commits, builds `url_for("features.referral_landing", code=..., _external=True)`, dispatches email. Wrapped in try/except — RSVP commit always succeeds even if email fails. Subject format: `You're going. {event.title}, {event.date %B %d}`. Smoke tests passed on fresh sqlite: 6 canonical dates seeded with correct time/location, idempotent re-seed (still 6), `_generate_upcoming_occurrences(template, weeks_ahead=8)` on the manual template returns 0, invalid `recurrence_rule` raises `ValueError("recurrence_rule must be in [...], got 'not_a_real_value'")`, RSVP `going` captured email contained event title + `Thursday, May 14, 2026 - 6:30 PM EST` + `The Temple, 155 8th Street North, Saint Petersburg, FL 33701` + `/r/<auto-generated-code>`, RSVP `interested` did NOT fire email, brand-new user (no referral_code) had one auto-generated by `ensure_referral_code()` and the URL contained it. NOT in scope: `.ics` calendar attachments, day-of reminder email, GHL engagement-tagging on RSVP, SMS — all Phase 4. Completed 2026-05-09. |
| 14 — Team auto-post queue + every-other-day cron | ✅ done | New `TeamPostQueue` model (migration `9926b13b9552`, chains off Phase 11.5 `c9d1f3a8b6e2`) — one row per future Team post, status vocab `pending|published|skipped` enforced via `@validates`, FK to `space.id` (nullable=False, indexed) + `post.id` (nullable, populated on publish). New `flask cron team-post-publish` command in `cron.py` — per-Space cadence check (`TEAM_POST_CADENCE_DAYS` env, default 2): a Space publishes the next-FIFO queued row only if its most recent Team post is older than the cadence threshold. Strict FIFO by `(queue_position ASC, created_at ASC)`. One post per Space per run. Reads "last team post" from the `post` table (not the queue) so the 123 backdated Phase 12 reseed posts naturally respect cadence. Published rows are retained for audit (status flips `pending` → `published` with `published_post_id` + `published_at` set; rows are never deleted). Does NOT fire notifications, GHL pushes, or engagement tagging (Phase 7/iOS-shell concern). New `scripts/team_post_enqueue.py` — single-post via stdin or `--file`, bulk via `--bulk-file` (one post per `=====` separator, strips leading `#` header lines to match `seed_content/*.txt` format). Appends to per-Space queue at `max(queue_position) + 1`. New admin route `/admin/team-queue` + `templates/admin_team_queue.html` — per-Space pending count, last-published timestamp, next-in-queue preview, and a "skip next" form (POST `/admin/team-queue/skip/<space_id>` flips the next pending row to `skipped` without publishing). Edit/delete UI not added — admins use `flask shell` if needed. Smoke tests on fresh dev sqlite (with team user fixture + a 6h-old team post seeded in every Space + 19 queue rows enqueued via stdin + bulk-file): cron with cadence not up → `cadence=2d published=0 skipped_recent=8 skipped_empty=0`; backdate `Sovereign Wealth` team post to 5 days ago → cron `published=1 skipped_recent=7 skipped_empty=0`, new Post id=45 created, queue row 1 flipped to `status='published'` with `published_post_id=45` and `published_at` set; re-run cron immediately → all 8 spaces `skipped_recent` (just-published post is now < 2 days old); cron idempotent (3 runs in a row, net effect = 0); admin GET `/admin/team-queue` returns 200 with all 8 Space rows; non-admin GET → 403; admin POST `/admin/team-queue/skip/<space_id>` flips next pending row from `pending` → `skipped` (count decremented); `TeamPostQueue(status='bogus')` raises `ValueError("team_post_queue.status must be in ['pending', 'published', 'skipped'], got 'bogus'")` (both at constructor and setattr). NOT in scope: migrating the 123 already-published Team posts into `team_post_queue` (they stay in `post`); a draft/review workflow on queued rows; cross-Space deduplication; auto-populating the queue from `seed_content/*.txt`; GHL/Stripe/R2 changes. Completed 2026-05-11. |

---

## 9. Decisions Log

- **2026-05-02 — Business model locked.** Ongoing $99/month subscription, no fixed end. Lifetime access is earned via referrals: when 3 of a member's referrals EACH complete 6 paid months (6 × $99 = $594 per referral), the referring member's billing stops permanently and access becomes lifetime. The `User.payments_made_count`, `User.qualified_referrals_count`, `User.lifetime_access`, and `User.lifetime_qualified_at` fields in `models.py` already reflect this model — no schema change needed. Phase 2 (Stripe → GHL webhook) and Phase 1 (GHL pipelines/tags) must encode this rule: tag `lifetime-qualified` when `lifetime_access` flips to true; tag referrals on each successful payment milestone (1, 3, 6 paid months) so referrers get progress notifications.
- ~~**2026-05-02 — Free trial model locked.**~~ **SUPERSEDED 2026-05-03 — Trial removed entirely.** Per Kashi: brand voice favors "premium, no soft-sell, real men commit" over discount-y trial offers. Actions taken in same-session manager work: (1) created new Stripe price `price_1TSstaCl2bd6Dz0IKKuohZuP` ($99/mo recurring, NO trial_period_days) on the same product `prod_URkIzQKGVwanyy`; (2) updated Railway `STRIPE_PRICE_ID` env to point at new price; (3) archived old trial-bearing price `price_1TSqoGCl2bd6Dz0I44Po79vj` (set `active=false`); (4) removed trial banner from `templates/pricing.html`; (5) rewrote 3 FAQ items in `templates/landing.html` to drop trial language; (6) rewrote `templates/legal.html` §2 (Membership & Billing) and §2A (Lifetime Access) to drop trial mechanic + the "trial-period months don't count" exclusion clause. Net: every Stripe-path signup is now charged $99 on day 1; `User.subscription_status` will go straight to `"active"` (the Phase 0 fix to accept `"trialing"` becomes defensive-only — never exercised). The `trialing` and `trial-cancelled` stage tags in `lib/ghl.STAGE_TAGS` remain valid (no breaking change to lib) but become unreachable in normal flow. Original locked trial text retained below (struck through) for audit trail in git history.

- ~~**2026-05-02 — Free trial model (now superseded — see entry above).**~~ Original text: 30-day free trial, **card required at signup** (Stripe `subscription_data.trial_period_days = 30`). Auto-bills on day 31. No "free account without card" path — every signed-up user has a payment method on file. Implications: (1) Stripe subscription status during trial is `"trialing"`, NOT `"active"` — Phase 0 must update `User.has_active_subscription` in `models.py:120-126` to accept both `"active"` and `"trialing"` or trial users will be paywalled out immediately. (2) §7 (Customer Journey) updated to add `trialing` stage and split `cancelled` into `trial-cancelled` (day 1–30, never charged) vs `cancelled` (paid at least once). These are different win-back audiences with different messaging. (3) `User.payments_made_count` only increments on successful real charges (day 31+), so trial months do NOT count toward referrer qualification — this is intentional and aligns with the locked business model.
- **2026-05-02 — Canonical brand positioning locked from existing landing.html.** No new copy will be drafted. The hero ("For the men who refuse to be average."), eyebrow ("Sovereign Society — by application only"), manifesto ("engineered reality" → "build the fire"), 6 pillars (Deal Flow / Accountability / Mastermind Calls / The Vault / The Map / The Boardroom), tier names (Bronze/Silver/Gold/Platinum), and final CTA ("The door is open. Walk through it.") in `templates/landing.html` ARE the brand voice. Future copy work pulls from this source. Voice rule: no sobriety/consumption framing. **Discovered during this lock**: landing.html FAQ + pricing.html + legal.html all describe a DIFFERENT business model than what was locked earlier (they say "$99/mo for 3 months → lifetime"). All three need rewriting to match the actual locked model — that's Phase 0A.
- **2026-05-02 — Phase 0A scoped.** Three files have stale/wrong public copy that must be reconciled with locked Q1+Q2 before any marketing or signup activity: `templates/landing.html` (FAQ #2/#5/#6), `templates/pricing.html` ($100 typo → $99, add trial + lifetime referral mechanic), `templates/legal.html` Terms section 2 (Membership clause currently misrepresents the offer — material contract-law exposure). Phase prompt written at `phase-prompts/phase-0a-copy-terms-alignment.md`. Per Kashi: agreement must explicitly disclose the 6-month commitment math per referral.
- **2026-05-02 — ICP locked (TENTATIVE — revisit after first ad campaign data).** Per Kashi: "go with whatever sounds best for now and we can always fix this later." (1) **Primary trigger pain: disillusionment / awakening** — buyer recently red-pilled on food/money/media/state, can't unsee it, looking for a brotherhood of men who already see it. Aligns with the existing manifesto voice on landing.html. Acquisition lives in sovereignty-adjacent creator audiences (RFK Jr / Tucker Carlson / Joe Rogan / Calley Means / Bret Weinstein-adjacent). (2) **Secondary trigger pain: empty circle** — D-buyers typically lose their old social ties; this is the soft-landing copy angle. (3) **Age band: 30–50.** Below 30 can't afford $99 + 3 referrals; above 50 is harder cold-traffic. (4) **Income band: $100k–$500k/year.** Below can't refer; above is already in YPO/EO. (5) **Geography: US primary, UK/CA/AU secondary, non-English markets skip for v1.** Why NOT pain (B) deal-flow / (C) fathers / (E) plateau-successful: B too narrow, C requires manifesto pivot, E is overcrowded with men's-coach competitors. **Revisit trigger:** after the first $5–10k of ad spend, look at which creator audiences and which copy variants converted and re-lock based on real data.
- **2026-05-02 — Day-1 launch-blocker feature set locked (TENTATIVE).** Per Kashi: "fix later" delegation. **Must-work-end-to-end (any bug = launch blocker):** signup → Stripe trial → paywall access flow (incl. `has_active_subscription` fix); feed post/comment/like; DMs; Spaces (join/post/chat — 6 already seeded); profile editing; Stripe billing portal + cancellation; Member Map (distinctive, sells the brand); Events / Mastermind Calls RSVP (1–2 seeded events at launch); notifications; referral system (it IS the business model); pricing + Terms pages (Phase 0A). **Must-work-but-launch-empty (organic content fills):** Deal Board, Resource Vault, Wins Wall, Accountability goals. **Nice-to-have-skippable-at-launch:** Stories, Reels, Weekly Challenges, AI Wingman, Badges (compute lazily), Activity feed, full-text search. Phase 0 audit confirms which of these actually work end-to-end vs which need fixes. Phase prompts for individual fixes will reference this list.
- **2026-05-02 — Phase 0B v1 → v2 revision. Canonical Spaces locked at 8, canonical Events at 3.** Phase 0B v1 prompt assumed CLAUDE.md's documented 6 Spaces. v1 executor stop-and-reported because the live DB actually has 14 Spaces (legacy 6 from a pre-rebrand seed run + new 8 from the current `_seed_content()` in `app.py:281-309`) and 8 Events (legacy 5 + current 3). Per Kashi: kill the legacy 6 Spaces and legacy 5 Events; canonical set is **the 8 sovereign-pilled Spaces** (Sovereign Wealth, Body & Iron, Awake Minds, Brotherhood Ops, The Arsenal, Red Pill Intel, Family & Legacy, Off Grid) and **the 3 sovereign-voiced Events** (Fire to Fire, Sovereign Wealth Workshop, Brotherhood Summit). Side effect: pre-existing `cover_image` path bug on the legacy 6 (`templates/spaces.html:16` resolved bare filenames against `/static/` while files lived in `/static/uploads/`) auto-resolves when legacy is deleted and new banners use full relative paths (`img/seed/space-<slug>.png`). v2 prompt at `phase-prompts/phase-0b-content-seeding.md` overwrites v1; git history preserves v1. v2 also includes a one-line `_seed_content()` idempotency-guard verification + fix (only if needed) so the DB doesn't grow on each boot, and updates `CLAUDE.md` Community Spaces + Recurring Events sections to reflect the canonical set.
- **2026-05-02 — Phase 0B complete (commit 78f8f47).** Community seeded, legacy purged. **Migrations:** legacy 6 Spaces (The Vault / Business Strategy Room / Networking Lounge / Investment Club / Wellness & Health / Creator's Corner) deleted; legacy 5 Events (Weekly Mastermind Call / Monthly Networking Mixer / Guest Speaker: AI Automation / Deal Flow Friday / Wellness Workshop: Peptide Protocols) deleted; 6 orphan PNG files in `static/uploads/` removed. Live DB went from 14→8 Spaces and 8→3 Events. **Seeded:** 8 placeholder users (Marcus W., James R., Sean T., Brendan M., Kyle H., Anders L., Chase W., David K.) at `seed.<slug>@sovereign.placeholder` with shared dev password `ChangeBeforeLaunch_2026!`; tier mix is 1 Platinum / 3 Gold / 3 Silver / 1 Bronze; cities span US for Member Map distribution. 16 posts (2 per canonical Space), 4 wins (Win cover wired), 4 deals (Deal cover wired, mix of investment/partnership/service/hiring), 7 resources (with 14 upvotes), 1 active "7-Day Cold Plunge Discipline" challenge (+3 submissions), 14 RSVPs across the 3 canonical events. **Imagery:** 13 abstract gold-on-black PNGs via Nano Banana (8 Space banners + 3 Event covers @ 16:9 1K, 2 generic content covers @ 1:1 1K) saved to `static/img/seed/`; 8 SVG monogram avatars (gold initials on black) generated locally. All `cover_image` fields use full relative paths (`img/seed/space-<slug>.png`) so `url_for('static', ...)` resolves correctly — also implicitly resolves the legacy `templates/spaces.html:16` path bug. **`_seed_content()` idempotency:** verified correct as-is at `app.py:281-396` (existence-checks via `Space.query.filter_by(name=...).first()`, same for Events and Posts) — no fix needed, `app.py` not touched. **CLAUDE.md** Community Spaces + Recurring Events sections updated to canonical 8/3. **Hygiene:** `seed_placeholders.py` is fully idempotent (re-run is no-op) and supports `--delete` flag that wipes all `seed.*` users (cascade), the active challenge, and every image in `static/img/seed/`. Smoke test on local Flask (port 5000 — Control Center wasn't actually holding it tonight; PORT=5050 ignored by `app.py:1709` hardcode, see §10 risk): `/feed`, `/spaces`, `/wins`, `/deals`, `/events`, `/members`, `/resources`, `/map`, `/space/<id>` all return 200 when logged in as a placeholder; all 8 canonical Space banners render via `<img>` tag in `/spaces`.
- **2026-05-02 — Phase 0 audit complete (bug-fix commit `1116d92`; audit-report commit follows immediately).** Two 1-line launch-blocker bugs fixed: `app.py:1709` now reads `port=int(os.environ.get("PORT", 5000))` (Railway sets `PORT`; macOS Control Center holds 5000 locally so the env-var path is needed in dev too); `models.py:120-126` `has_active_subscription` now accepts `"active"` OR `"trialing"` (locked 30-day trial model would have paywalled out every new signup). Discovery surfaced **three new launch blockers** that were not in §10 before — see §10 additions: (1) production at `https://onepercentmensclub.up.railway.app` returns Railway-edge 404 fallback (`x-railway-fallback: true`) on every probed path — service is unbound or paused; nothing is currently serving the app; (2) `cron.py:64,75-77,82` references `Win.content` and `Event.starts_at` neither of which exist on the actual models (Win has `title`+`description`, Event has `date`+`time`) — the weekly digest CLI command will throw `AttributeError` on first run; (3) GHL tags are split between legacy `"ABMC"` and new `"Sovereign Society"` strings across 4 call sites — Phase 1 must standardize before any lifecycle automations are wired. Also a **factual correction to §6**: prior text said "Stripe webhook handler does not push to GHL" — that is no longer true. The webhook DOES push to GHL on subscription cancel (Churned) and on lifetime unlock (Lifetime). It does NOT push on checkout-completed, sub-updated, payment-succeeded (non-lifetime branch), or payment-failed; Phase 2 widens the coverage. Routes inventory locked at 105 (54 in `app.py`, 9 in `phase3_routes.py`, 42 in `features_routes.py`) — full list in §13.1.
- **2026-05-02 — Phase 0C: founder-code onboarding semantics locked.** A valid founder code (entered on `/pricing`'s "Have a code?" panel) grants the entered email a fully-active lifetime account with no payment and no email-verification step: `User.lifetime_access=True`, `subscription_status="active"`, `lifetime_qualified_at=datetime.utcnow()`, `email_verified=True`, `is_admin=False` (admin status remains a manual flip in `/admin`, NOT carried by the code itself — keeps the code from being an admin-escalation vector if leaked). Code source: single `_is_founder_code()` helper in `app.py` reads `FOUNDER_CODES` (or legacy singular `FOUNDER_CODE`) with default `ABMC2026`; `/validate-code` and `/signup-with-code` both go through the helper. UX: when validate-code returns valid, pricing.html swaps the email field for a 3-input mini-form (name/email/password) and rebinds the main CTA via `dataset.founder='true'` to call `/signup-with-code`; Stripe path stays untouched. GHL push uses legacy tags `["Lifetime", "ABMC"]` — consistent with the rest of current call sites; Phase 1 will sweep all four call sites to the canonical `lifetime-qualified` taxonomy in one shot. Welcome email reuses `send_lifetime_unlocked` (template fits semantic state); RESEND degrade path prints to stdout (graceful). CSRF: `require_csrf` decorator extended to also accept `X-CSRFToken` header so JSON fetch callers don't need to form-encode; `templates/base.html` now carries a `<meta name="csrf-token">` tag for client-side reads. Rate limit: `5/min` per IP, matches `/signup`'s posture. Implementation commit: phase-0c shipped on main.
- **2026-05-02 — Phase 1 complete (commits `99cc51b` + `e5ffdaf` + backfill/SoT commit).** GHL lift + tag taxonomy + cron.py fixes + threaded-email-render fix shipped together. **Lift:** `lib/__init__.py` + `lib/ghl.py` created; the inline `ghl_upsert_contact` (formerly `app.py:227-258`) and its `GHL_API_KEY`/`GHL_LOCATION_ID` module-level constants are gone; orphaned `requests` and top-level `threading` imports removed. New surface: `ghl.upsert_contact(*, email, name, phone=None, stage_tag=None, custom_fields=None, extra_tags=None)`, `ghl.upsert_opportunity(*, contact_email, stage_tag, monetary_value=99.0)` (Phase 2 stub), `ghl.custom_fields_from_user(user)`, `ghl.STAGE_TAGS`. Daemon-thread send + fail-silent + env-unset no-op preserved. Validation runs BEFORE the env-unset check so misuse fails loudly even in dev. **Tag taxonomy locked at 9 stage tags:** `prospect`, `trialing`, `active-member`, `power-member`, `lifetime-qualified`, `at-risk`, `trial-cancelled`, `cancelled`, `reactivated`. Brand tags (`ABMC`, `Sovereign Society`, `Founder`, `Paid Member`, `Churned`, `Lifetime`) are explicitly rejected with `ValueError`. Stage tags routed via `extra_tags=` are also rejected (caller-mistake guard). **5 call sites migrated:** (1) `/signup` → `prospect`; (2) `/signup-with-code` → `lifetime-qualified` + `custom_fields`; (3) `/subscription/success` → `trialing` + `custom_fields`; (4) `customer.subscription.deleted` → `trial-cancelled` if `payments_made_count == 0` else `cancelled` + `custom_fields`; (5) `invoice.payment_succeeded` non-lifetime → `active-member` + `custom_fields` (NEW — Phase 0 audit gap closed; Phase 2 widens further); (5b) `invoice.payment_succeeded` lifetime branch → referrer gets `lifetime-qualified` + `custom_fields` and the paying user gets the `active-member` push from the same handler. **4 custom fields sync** (`payments_made_count`, `qualified_referrals_count`, `lifetime_access` as `"true"`/`"false"`, `lifetime_qualified_at` as ISO date or empty). **`cron.py` 3 bugs fixed:** `_build_digest_data` builds `f"{w.title} - {w.description}"` for the dict's `content` key (the template still reads `content`); `Event.starts_at` swapped to `Event.date` filter/order with `Event.time` appended to the display string; `cli_test_email` does an explicit early-return + `click.echo` when no admin exists. Smoke-tested locally on Flask 3.0 / port 5099: all 9 prompt smoke tests pass; logged-in `/feed` and `/api/notifications/unread-count` both 200; `flask cron digest` runs to completion (with `SERVER_NAME` set for the URL render); `flask cron test-email` prints the friendly message. **`SERVER_NAME` + `PREFERRED_URL_SCHEME`:** added to `app.config` near MAX_CONTENT_LENGTH; defaults to None in dev (preserves localhost binding) and `anti-billionaires-app-production.up.railway.app` in production unless `SERVER_NAME` env var overrides. **Backfill script:** `scripts/backfill_ghl_tags.py` shipped (default `--dry-run`, requires `--apply` for actual writes; `--throttle-ms` cushion); `scripts/README.md` documents it. NOT executed against live GHL — Kashi runs `--apply` after Bryce shares location creds. **What did NOT happen this phase (Phase 2/4 scope):** webhook coverage on `checkout.session.completed`, `customer.subscription.updated`, `invoice.payment_failed`; engagement-tagging (post/win/event); live GHL workflows; pipeline / stage IDs (env-var contract is in place but values not yet set on Railway). **Net surprise discovered during Step 3.5:** `/create-checkout-session` (`app.py:1348`) does NOT pass `subscription_data={"trial_period_days": 30}` — every Stripe-path signup auto-bills on day 1, contradicting the locked 30-day trial model and the public Terms §2 / FAQ #2. Logged in §10 as a fresh launch blocker. **[FALSE ALARM — RESOLVED SAME DAY]:** Manager session verified the Price object itself (`price_1TSqoGCl2bd6Dz0I44Po79vj`) carries `recurring.trial_period_days = 30` — Stripe applies the trial automatically when Checkout creates a subscription against that price. No code change needed. §10 entry struck through with full provenance.
- **2026-05-02 — Post-Phase-1 vision dump (Kashi).** Captured here so future-phase scoping aligns with intent. Five threads: **(A) Stripe wiring complete in same session** — product `prod_URkIzQKGVwanyy`, price `price_1TSqoGCl2bd6Dz0I44Po79vj` ($99/mo + 30d trial baked in), webhook `we_1TSqoXCl2bd6Dz0I2UUL0hHy` posting all 5 lifecycle events to `/webhook/stripe`. All 4 Stripe env vars set on Railway. Live mode (`rk_live_*`, restricted key from Bryce's account). Manager created the product+price+webhook via Stripe API directly using the restricted key (write scopes confirmed). **(B) Phase 4 — Events stack:** every-other-Thursday recurring event at **Revo Wellness** (address still TBD from Kashi); auto-create future occurrences; lunch location is a per-event dynamic field; open `phase3_routes.create_event` to non-admin members (currently admin-only) with a soft trust-tier gate; RSVP click → confirmation email + calendar invite (.ics); reminder email day-of. **(C) Phase 5 — AI Wingman premium directory (NEW phase, not in original 0–7 plan):** searchable member directory by profession; AI Wingman query "anyone here a [profession]?" returns matches + intro affordance; **upcharge mechanism TBD** ("we have to figure out what that looks like" — premium tier vs per-query); profession-data capture point also TBD (signup form? profile edit? LinkedIn import?). **(D) Phase 6 — Referral celebration UX:** dashboard tracking already partial (`payments_made_count`, `qualified_referrals_count` on User); add community-wide announcement when a referral signs up ("Congratulations Timmy, you're brother #5 in for [referrer]"); personalized **rotating** welcome perk ("free ice bath at Revo," etc.) — needs a perk pool + rotation logic; this refines the original "Customer Journey Playbook" Phase 6 scope into something more concrete. **(E) Phase 7 — iOS / Android shell app (Capacitor — already partially scoped):** `capacitor.config.json` exists; bundle `com.onepercentmensclub.app` (intentionally legacy per CLAUDE.md); `server.url` updated 2026-05-02 to current Railway URL; iOS needs Xcode project + Apple Dev enrollment + APNs `.p8` + App Store Connect listing; Android needs Google Play Console + FCM; push notifications: `DeviceToken` model + `/api/devices/register` already exist on the backend, just need front-of-stack wiring. **GHL pipeline + workflow setup is implicit in all of A–D** — manager will create pipeline + 7 stage IDs via GHL API once Bryce shares the `pit-` token + location ID (memory: a token was pasted in a prior session, not currently in Railway env, treat as needing fresh paste).
- **2026-05-03 — Onboarding UX fix: deduped photo capture + mobile spaces grid.** Two issues spotted on iPhone signup walkthrough: (1) `templates/signup.html` had an "optional" Profile Photo field, and `templates/onboarding.html` step 1 ("Show your face") then demanded the same photo immediately after — users were prompted twice in a row. (2) The 5-step onboarding card rendered with desktop-tuned padding (56px × 48px) and a 2-column `ob-spaces` grid; on a ~390px viewport this squeezed each space card to ~135px, wrapping "Sovereign Wealth" / "Body & Iron" headers and breaking visual hierarchy on the "Pick your spaces" step. **Fix:** removed the photo upload field (and its now-orphaned `enctype="multipart/form-data"`) from `templates/signup.html` so onboarding step 1 is the single, required capture point; added a `@media (max-width: 600px)` block to `templates/onboarding.html` reducing card padding to 32×20, h1 to 30px, collapsing `ob-spaces` to a single column, and stacking the action buttons (Skip + Continue) vertically. **Why safe:** both `/signup` (`app.py:771-775`) and the Stripe-path `/subscription/success` reuse handler (`app.py:1395-1399`) treat `profile_photo` as optional (`None` if no file), and `ghl.custom_fields_from_user` does not push `profile_photo`, so removing the signup field has zero downstream impact on user creation, GHL sync, or the `prospect`/`trialing` stage tags. Onboarding step 1 itself is unchanged (still wired to `/onboarding` POST handler at `app.py:517-522`). Not yet smoke-tested on a mobile viewport — Kashi to verify on next iPhone walkthrough.
- **2026-05-02 — Phase 0A complete (commit e4aa7fc).** Public copy + Terms reconciled with locked business model. Three files edited: `templates/landing.html` (FAQ #2/#5/#6 rewritten), `templates/pricing.html` ($100→$99, +trial banner under tagline, +lifetime referral note under cancel-anytime), `templates/legal.html` (§2 expanded into Membership & Billing, new §2A Lifetime Access — Referral-Earned). Repo-wide grep for residual stale copy (`99/month for|three months|three payments|3 payments|3 months`) returned clean. Verified live on local Flask (port 5050; macOS Control Center holds 5000) — `/`, `/pricing`, `/terms` all 200; section ordering reads 1, 2, 2A, 3, 4, 5, 6, 7, 8 as specified. **Final FAQ #2 wording (canonical, quote verbatim in future copy):** "$99 / month, ongoing. Your first 30 days are free — you sign up with a card on file but no charge hits until day 31. There is no fixed end to the membership; you can cancel any time. The Society also offers Lifetime Access — but you don't buy it, you earn it. Bring three brothers into the Society. Once each one has completed six paid months ($594 each), your billing ends permanently and your seat is yours for life." **Final Terms §2A wording (canonical, contract-law load-bearing):** "Sovereign Society offers Lifetime Access at no further charge to members who successfully refer at least three (3) other paying members, where each of those three referrals must independently complete six (6) consecutive months of $99 payments — a total of $594 paid by each referral, or $1,782 in total referral-driven revenue, before the referring member qualifies. Once you reach this threshold, your $99 monthly billing will be permanently terminated and your platform access becomes Lifetime, contingent on continued adherence to the Member Code of Conduct (Section 4). The following do NOT count toward referral qualification: trial-period months (no payment was made); refunded payments; charged-back payments; payments made by referrals who later cancelled before reaching six successful payments. If a referral cancels before completing six payments, their progress does not transfer and is not bankable for a future re-signup. There is no other path to Lifetime Access. Statements elsewhere on the website that may have suggested an alternative path are superseded by this Section."
- **2026-05-03 — Phase 2 complete: Space cover images shipped to prod.** Manager session surfaced that `/spaces` on production rendered placeholder grids for all 8 canonical Spaces despite the cover PNGs being deployed in `static/img/seed/`. Direct query against prod Postgres confirmed every Space row had `cover_image=NULL`. **Root cause:** `_seed_content()` at `app.py:271` created the 8 canonical Spaces but never wrote `cover_image`. The cover-wiring logic only existed in the dev-only `seed_placeholders.py`'s `update_space_covers()`, which has never been run against prod (it lives behind `python seed_placeholders.py` and the script doc explicitly warns against running with placeholder users intact in prod). Phase 0B's "all `cover_image` fields use full relative paths" claim in the 2026-05-02 entry was true *for the local dev DB after `seed_placeholders.py` ran* — the entry should have been read as dev-state, not prod-state. **Fix (commit follows immediately):** `_seed_content()` `spaces_data` extended from `(name, desc)` tuples to `(name, desc, cover_image)`; loop now branches on `Space.query.filter_by(name=name).first()` — if existing row has NULL `cover_image`, backfill it; if no row, insert with `cover_image` populated. **Self-heal pattern locked:** `_seed_content()` only fills NULLs and never overwrites a non-NULL `cover_image` set by an admin via the UI, so future manual cover changes are durable across reboots. Cover paths use the same `img/seed/space-<slug>.png` mapping that `seed_placeholders.py:103-112` already encoded. No schema change. **Smoke verified locally:** test (a) NULL all covers on existing DB → boot → all 8 backfilled; test (b) fresh DB + admin user → boot → all 8 inserted with covers. Production verification pending Railway redeploy after push. **Files touched:** `app.py` (one block in `_seed_content`), `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry). `templates/spaces.html`, `models.py`, `seed_placeholders.py` untouched. Disjoint from concurrent Phase 3 (events stack rework).
- **2026-05-03 — Phase 3 complete: Events stack rework shipped.** Replaces the 3 stale Phase-0B one-off events with a typed, recurrence-aware stack. **Vocabularies LOCKED** (executor must validate against these — `frozenset` enforced via `@validates` on the model; raises `ValueError` on anything else): `event_type ∈ {chapter_recurring, weekly_recurring, member_meetup, official_one_off}` (the last is reserved for forward-compat — annual Brotherhood Summit lives there once it's reseeded later) and `recurrence_rule ∈ {none, every_thursday, first_and_last_thursday_monthly}`. Two recurrence rules cover everything Kashi specified — explicitly NOT building a general RFC 5545 RRULE engine. **Recurrence model:** each calendar occurrence is its own `Event` row pointing back at a `is_recurrence_template=True` parent via `recurrence_parent_id`. Per-occurrence rows preserve all existing `EventRSVP.event_id` joins — RSVPs can be attached to "the May 7 Thursday lunch" specifically without breaking when next week's lunch is added. Templates do NOT show in `/events` lists; only their generated children do. **Generator:** `_generate_upcoming_occurrences(template, weeks_ahead=8)` in `app.py` walks weekday math forward from `today`, builds the candidate date set, queries existing `(recurrence_parent_id, date)` pairs, and inserts the diff. Idempotent — safe to call on every `/events` GET (which `phase3_routes.events` does at the top of the route). Children inherit `title/description/time/location/chapter/host_id/cover_image/event_type` from the template and override `recurrence_rule="none"`, `is_recurrence_template=False`, `recurrence_parent_id=template.id`, `date=<computed>`. Time/location can be edited per-occurrence after generation (admin UI for this is the existing `/events/create` admin path; per-occurrence-edit UI is a future enhancement). **Seed (locked):** 3 stale events (`Fire to Fire - St. Pete`, `Sovereign Wealth Workshop`, `Brotherhood Summit`) wiped on every `_seed_content()` boot — pre-real-traffic, no real RSVPs lost; cascade drops seed RSVPs along with each row. New seeds: (1) `St. Petersburg Chapter Biweekly` — `chapter_recurring`, chapter=`St. Petersburg, FL`, `first_and_last_thursday_monthly`, anchor = first Thursday of current month, time `6:30 PM EST`, location `The Temple, 155 8th Street North, Saint Petersburg, FL 33701`; (2) `Thursday Group Lunch` — `weekly_recurring`, chapter=`Global`, `every_thursday`, anchor = next Thursday, time/location blank (admins fill per occurrence — alternates each week, do NOT rotate through a preset list). After seed: 14 events total (2 templates + 4 St. Pete biweekly occurrences over 8 weeks + 8 Thursday lunch occurrences). **Schema:** migration `b1e2f5a6c7d8` adds 5 columns to `event` (`event_type`/`chapter`/`recurrence_rule`/`recurrence_parent_id`/`is_recurrence_template`) + index on `recurrence_parent_id` + self-FK. Down-rev `e4f5a6b7c8d9`. **Routes/templates:** `phase3_routes.events` groups by chapter (chapters_grouped dict, currently 1 chapter — St. Pete — but template iterates so adding Austin etc. just works), then weekly, then member meetups; empty-state CTA "Post a Meetup" on the meetups section. `phase3_routes.create_event` opened to any `has_active_subscription` member via local `_paywall_required` (mirrors `app.paywall_required` to dodge the existing `app.py → phase3_routes` import cycle); non-admin POST is server-side-forced to `event_type=member_meetup`, `recurrence_rule=none`, `is_recurrence_template=False` regardless of submitted form values. `templates/event_create.html` (note: file is named `event_create.html`, NOT `create_event.html` as the prompt's pseudocode said — kept the existing name) hides the `event_type` and `recurrence_rule` selects from non-admins; admin sees both. `templates/event_detail.html` adds a "Part of: <template title> (recurring)" line above the date when `recurrence_parent_id` is set, plus a chapter line. `event_rsvp` route also gains `_paywall_required`. **Smoke tests passed (local fresh sqlite DB):** seed produces 14 rows with the right counts; idempotent re-seed (count stays 14); three sections render with no stale event titles; admin "Austin Chapter Biweekly" create POST produces 4 occurrences (template + child rows wired to `recurrence_parent_id`); member bypass POST (`event_type=chapter_recurring`+`recurrence_rule=every_thursday`) is forced to `member_meetup`/`none`/`is_recurrence_template=False`; RSVP attaches to the occurrence (`EventRSVP.event_id=7`) NOT the template (id=2); `Event(event_type="not_a_real_type")` raises `ValueError: invalid event_type: 'not_a_real_type'`; `Event(recurrence_rule="monthly_first")` raises `ValueError: invalid recurrence_rule: 'monthly_first'`. **Production verification:** pending Railway redeploy after push. **NOT in scope (deliberately deferred):** `.ics` calendar invites; RSVP confirmation/reminder emails; engagement-tagging on RSVP via `lib/ghl.py` — all Phase 4 work. No general RRULE engine. No Chapter model — `chapter` stays a free-form string for now. No background scheduler — recurrence generation runs on-demand from `/events` GET (cheap, idempotent existence check). **Concurrent-phase migration collision:** my migration originally collided with two sister-phase migrations on revision id `f5a6b7c8d9e0` (uncommitted local files for `location_visibility` from Phase 6 + `assessment_models` from Phase 5 since renamed to `f5a6b7c8d9ec`). Renamed mine to unique `b1e2f5a6c7d8` to avoid the clash; only my migration file is staged in the Phase 3 commit (per Step 7 stage list). The multi-head reconciliation across the parallel phases will need a merge-migration when the manager threads them together — flagging in §10 below.
- **2026-05-03 — Phase 5 complete: Member Self-Assessment shipped.** New 40-question Sovereign Code self-assessment now gates the post-signup flow ahead of the 5-step onboarding. **Pillar order locked** (matches the Phase 4 landing-edits memory verbatim): Purpose, Strength, Wealth, Brotherhood, Family, Faith, Awareness, Control. **Question text locked** — 40 strings copied verbatim from `~/.claude/projects/-Users-kenneth-anti-billionaires-app/memory/project_pending_assessment_feature.md`; centralized in `lib/assessment.py` so future content changes are one-file. **Likert scale locked** at 1=Not at all → 5=Completely with no default selection (forces honest pick). **Schema:** `AssessmentResponse` table (`id`, `user_id`, `submitted_at`, `answers_json`, `pillar_scores_json` — JSON strings, not normalized rows; see §10 risk) + new `User.assessment_complete` boolean column (server_default false). Migration `f5a6b7c8d9ec` (down-rev `e4f5a6b7c8d9`). Re-takes append a new `AssessmentResponse` row, never overwrite — history is data. **4 routes:** `/assessment` GET (form), `/assessment/submit` POST JSON (validates → writes row → flips flag → returns `{redirect, scores}`), `/assessment/results` GET (reads latest), `/assessment/skip` POST (flips flag without creating a row, then redirects to `/onboarding` — "I'll do it later," not permanent opt-out). **Insertion point:** new helpers `_post_signup_redirect(user)` (returns Flask Response) and `_post_signup_redirect_url(user)` (URL string, for JSON callers) chain `assessment → onboarding → feed` based on the two completion flags. Replaces the previous redirects in `subscription_success` (both existing-user and new-user branches) and `signup_with_code` (which formerly returned `{"redirect": "/welcome"}`). **Decoration deviation from prompt:** the spec's `@paywall_required` was NOT used on assessment routes because that decorator force-redirects to `/onboarding` when onboarding is incomplete — which would loop assessment users straight past the assessment. Each route uses `@login_required` + manual `current_user.has_active_subscription` check (mirrors the existing `/onboarding` GET pattern). **Templates** match the `templates/onboarding.html` aesthetic (Cormorant Garamond serif, dark card on dark bg, gold #D4AF37 accents). `templates/assessment.html` is a single-page wizard with 8 hidden sub-sections, dot progress at top, JS pagination + per-question selected-state tracking, JSON fetch submit; "Skip assessment for now" footer link as the only escape hatch. `templates/assessment_results.html` shows 8 plain `Pillar — 4.2 / 5` rows with a callout that scores are private + "Begin onboarding" CTA. **Smoke tests:** GET /assessment 200; valid all-3s submit returns scores + `/assessment/results` redirect URL; missing pillar 400 with `{error:"missing pillar", pillar:"strength"}`; invalid value 6 → 400 with `pillar:"purpose", question_index:4`; CSRF enforced on submit (header `X-CSRFToken` accepted via the existing `require_csrf` decorator); results page reads latest of N responses; re-take creates row #2 with new scores while row #1 stays intact; score math sanity verified ([1..5] → 3.0, [5,5,5,5,5] → 5.0, [1,1,1,1,1] → 1.0). **What's deliberately NOT in scope:** matching/recommendations off scores (just stored, no consumers); public visibility of scores to other members (private to user); leveling/tier integration (no `User.points` change); charts/graphs (plain numbers); admin inspection UI; profile-page "retake" link UI (route is exposed; UI add-on is later). **Concurrent-phase note:** three other untracked migrations exist locally (`f5a6b7c8d9e0_events_type_chapter_recurrence`, `f5a6b7c8d9e0_location_visibility`, `f5a6b7c8d9ea_checklist_item_slug`) from Phases 2/4/6 sessions — they share the down-rev `e4f5a6b7c8d9` and two of them collide on revision `f5a6b7c8d9e0`. This Phase 5 migration uses unique revision `f5a6b7c8d9ec` to avoid colliding with mine; the multi-head reconciliation across all four parallel phases is a future merge-migration job, NOT this phase's responsibility (per Step 4: "Do NOT touch other phases' files").
- **2026-05-03 — Phase 6 complete: Member location search + proximity detection.** New "Find Brothers" surface lives at `/find`, sister to the existing Leaflet `/map` (which is unchanged). **Privacy model locked at 3 tiers:** `User.location_visibility ∈ {hidden, city_only, proximity_visible}`, default `city_only` for new users. Migration `f5a6b7c8d9e0` adds the column with `server_default='city_only'` and backfills `'hidden'` for any legacy `show_on_map=False` user (handles SQLite `0`/`false` and Postgres `false` in the same UPDATE). `show_on_map` column is kept for backwards-compat with `/map` and is mirrored (`!= 'hidden'`) on every visibility update so the existing Leaflet markers still respect the choice. **Geocoding:** `lib/geocoding.py` wraps Nominatim (OpenStreetMap, keyless, polite-use ~1 req/sec) with `functools.lru_cache(maxsize=1024)` + 5-second timeout + graceful None on any error. User-Agent header set to `Sovereign Society/1.0 (kashi@thebreathcoachschool.com)` per Nominatim policy. `haversine_miles(lat1, lon1, lat2, lon2)` ships in the same module; all distance math is Python (full table scan + Haversine filter — fine for <10K users; bounding-box pre-filter is a future optimization). **Routes (3 new + 1 supporting, all on the `features` blueprint):** `GET /find` (login_required + manual paywall gate to dodge the `app.py → features_routes.py` circular import — mirrors `app.paywall_required`), `POST /find/search` (accepts `{city, radius_miles}` JSON; geocodes; filters `User.location_visibility != 'hidden'` within the radius; never returns raw `lat`/`lng` — only city/country/miles/photo/name/id; sorts by distance), `POST /find/nearby` (accepts `{lat, lng}` from `navigator.geolocation`; filters `User.location_visibility == 'proximity_visible'`; returns 20 closest), and `POST /profile/visibility` (3-radio handler; rejects unknown values; mirrors `show_on_map`). Each route also excludes the requesting user from results and honors the 1–500 mi radius clamp on `/find/search`. **Templates:** `templates/find.html` is brand-consistent (Cormorant Garamond serif h1, gold-on-black, dark cards) with two CSS-toggled tabs ("Who's in your city" + "Brothers in your area"), 25/50/100 mi radius selector, and inline result cards (avatar, name, city, distance, click → `/profile/<id>`); errors and loading states render inline; CSRF read from the existing `<meta name="csrf-token">` tag. The 3-radio "Who can see this?" set is added to `templates/onboarding.html` step 3 below the country input (default `city_only`, processed by the existing `app.py:onboarding_submit` step-3 branch with the new vis whitelist) and to `templates/edit_profile.html` below the main edit form (its own POST → `/profile/visibility`). **Nav:** "Find Brothers" entry added to the avatar dropdown (between Welcome Checklist and My Bookmarks) and the mobile menu (next to Members). Per the prompt no checklist item was added — the welcome list is saturated. **Smoke tests** on local Flask 3.0 / port 5099 against a 4-user Austin fixture (proxA + proxB at `proximity_visible`, cityonly at `city_only`, hidden at `hidden`): Austin city search returns the 3 visible users (hidden excluded); `/find/nearby` from Austin coords returns only the 2 `proximity_visible` users; flipping proxA to `hidden` removes it from the city search; garbage city `"ksjdhfksjd"` returns 400 with `"could not locate that city"`; lru_cache cuts the second `Boise, ID` lookup from ~0.55s to ~0.005s; no regressions on `/map`, `/members`, `/profile/edit`, `/feed`; `POST /profile/visibility proximity_visible` flips the column and 302-redirects to `/profile/edit`, invalid value 302-redirects with a flash. **NOT in scope:** ripping out or modifying `/map` / `templates/member_map.html`; automatic geocoding on every profile save (only on explicit city searches); a new map library; native Capacitor geolocation plugin (browser API works inside the WebView — Phase 7+ if needed); real-time presence ("X is nearby right now"); leakage of `User.lat`/`lng` to other users via JSON response. **Concurrent-phase note:** four other untracked migrations exist locally (`b1e2f5a6c7d8_events_type_chapter_recurrence` shipped in same-day Phase 3; `f5a6b7c8d9ea_checklist_item_slug` from Phase 4; `f5a6b7c8d9ec_assessment_models` from Phase 5; my `f5a6b7c8d9e0_location_visibility`). Phases 3+4+5+6 each parented at `e4f5a6b7c8d9` so all four are independent heads. Multi-head reconciliation is a future `flask db merge` once each lands on main; this phase ships only its own files (per Step 4 "Do NOT cross over into other phases' files"). **Files touched (exactly):** `models.py` (one column on User), `migrations/versions/f5a6b7c8d9e0_location_visibility.py` (new), `lib/geocoding.py` (new), `features_routes.py` (3 new routes + visibility-update route), `app.py` (vis whitelist in onboarding_submit step 3), `templates/find.html` (new), `templates/edit_profile.html` (visibility section + form), `templates/onboarding.html` (radio block in step 3 + supporting CSS), `templates/base.html` (Find Brothers nav links), `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §8 row).
- **2026-05-03 — Phase 7 complete: Member-published projects shipped.** Members can now publish builds (businesses/products/missions) at `/projects`, sister to Deals. **Boundary locked:** Project ≠ Deal (transactional opportunity), Project ≠ Win (one-shot announcement), Project ≠ Space (group conversation). Project is creator-owned, has discrete state, and can grow over time via creator-posted updates. **Vocabularies LOCKED** (frozen sets enforced via `@validates` on the model — raises `ValueError` on anything else): `status ∈ {idea, building, launching, scaling, paused}`, `project_type ∈ {business, build, mission, cause, product}`, `visibility ∈ {members_only, brotherhood_only, private}`. **Visibility model:** `members_only` (default) — any active subscriber; `brotherhood_only` — Lifetime-Qualified members only (gated tier); `private` — creator-only (Phase 7+ surface, hidden from this phase's UI per scope). Helper `_visible_projects(query, user)` in `features_routes.py` is the canonical filter; ALL read paths (feed, detail, interest, profile) pipe through it. Self-view exception in detail/edit — creator always sees own active projects regardless of tier. **Schema:** migration `c1d2e3f4a5b6` (down-rev `f5a6b7c8d9e0` Phase 6 location_visibility — chains linearly, no merge needed). 4 tables: `Project` (user_id FK + indexed; `created_at`/`updated_at` with `onupdate=` so the feed can sort by recent activity), `ProjectUpdate` (project_id FK + indexed, creator-posted only — member discussion is OUT of scope per Step 4), `ProjectInterest` (UniqueConstraint on `(project_id, user_id)`), `ProjectPaymentMethod` (project_id FK + indexed, sort_order column for display ordering). **Payment methods (load-bearing legal scope):** 10 locked `method_type` values — eth, btc, sol, usdc_eth, usdc_sol, cashapp, venmo, paypal, stripe_link, custom_link. Each has an anchored full-match regex (`re.fullmatch`) applied at the route level for shape validation; the `@validates` on `method_type` only checks the type is in the dict, not the address shape. Soft cap of 10 methods per project. **SS does NOT process payments — directory only, off-platform.** Detail page renders a load-bearing disclaimer banner above the methods list: "Sovereign Society does not process these payments. You're transacting directly with the project creator off-platform. We do not custody, escrow, or refund. Use your own judgment and verify with the creator before sending." This wording (or close paraphrase) is required to keep SS out of money-transmitter / KYC / AML scope; future edits to the banner copy must preserve "does not process," "off-platform," and "no custody/escrow/refund" semantics. **9 new routes (all on `features` blueprint, all paywalled via inlined `_project_paywall_or_redirect()` to dodge the `app → features_routes` import cycle):** `GET /projects` (filterable feed by `status` + `type` querystring), `GET/POST /projects/create`, `GET /projects/<id>`, `GET/POST /projects/<id>/edit` (creator-only), `POST /projects/<id>/interest` (idempotent toggle — re-click removes the row; notification fires on first interest, never on un-interest), `POST /projects/<id>/update` (creator-only, 403 server-side for everyone else; bumps `project.updated_at` so the feed floats it), `POST /projects/<id>/archive` (creator-only, soft-delete via `is_active=False` — creator still reaches own archived project by direct URL with a banner; non-creators get 404 on detail and the project is hidden from feeds), `POST /projects/<id>/payment-method` (creator-only add), `POST /projects/<id>/payment-method/<pm_id>/delete` (creator-only remove). On non-admin Brotherhood-only POSTs, server-side downshifts `visibility` to `members_only` if the requester lacks `lifetime_access` (defense-in-depth — UI also hides the option). **Profile integration:** `app.py:profile` now fetches `Project.query.filter_by(user_id=user_id, is_active=True)` filtered by the same tier rules and passes `projects` to `templates/profile.html`, which renders a Projects section before Posts (gold-bordered cards, status + type pills, summary truncated at 120 chars). Empty state on own profile: "No projects yet. Share what you're building →". **Templates** (all brand-consistent, gold-on-black, Cormorant Garamond serif): `templates/projects.html` (feed with filter pills for status + type, empty state), `templates/project_detail.html` (cover image, pills, creator row, description, looking-for callout, interest toggle, interested-members chips, creator-only update form + updates timeline, payment-method directory with copy/open buttons + creator manage UI + the disclaimer banner above), `templates/create_project.html`, `templates/edit_project.html` (mirrors create + a danger-zone Archive button at the bottom). "Projects" added to base.html avatar dropdown (between My Deals and Referrals) and mobile menu (between Deals and Challenges). Per Step 4, no checklist 8th item was added (Phase 4's 7-item list is final). **Smoke tests** (Flask test client, per-user fresh clients to avoid SQLAlchemy session staleness across requests; setup in app_context, requests outside): all 4 vocab validators raise `ValueError` on invalid input; Bob (members_only sub) sees only members_only on feed and gets 404 on brotherhood_only detail; Cody (lifetime_access) sees both tiers; `/projects/<id>/interest` toggle round-trips correctly (interested=True then interested=False); non-creator gets 403 on `/update`, `/archive`, `/payment-method`, `/payment-method/<pm_id>/delete`; creator update succeeds (302) and inserts a `ProjectUpdate` row; ETH valid shape (0x + 40 hex) accepted, bogus "bogus" string rejected (no DB write); cashapp `$alice` accepted; archive flips `is_active=False`; archived project hidden from non-creator feed AND detail (404), still reachable by creator via direct URL with the "Archived" banner; profile integration: Bob sees no projects on Alice profile when p1 is archived + p2 is brotherhood_only, Cody (lifetime) sees the brotherhood_only project, Alice on own profile sees active-only (archived hidden). **GHL: zero pushes from new routes** (`grep -c "ghl\\." features_routes.py` = 0). **NOT in scope (deliberately deferred):** notifications ("X expressed interest in your project") — Phase 8; member-discussion comment threads on `ProjectUpdate` (creator-posts-only model is intentional for v1); GHL engagement-tagging on project create or interest expression — Phase 8; project-as-Deal-category (Deal Board stays separate — different model, different intent); `private` visibility UI surface (creator-only tier exists in schema, hidden from this phase's UI); no checklist 8th item (welcome list final at 7); no DM hooks ("Message this builder" — existing DM feature works once both members exist; not auto-wired here); no analytics, view-counts, or trending logic; no admin moderation surface; no sample seed projects (member-created from day one). **Files touched (exactly):** `models.py` (4 new model classes + 4 frozensets/dicts at the end after `AssessmentResponse`), `migrations/versions/c1d2e3f4a5b6_projects_models.py` (new), `features_routes.py` (Phase 7 imports + 9 new routes + `_visible_projects`/`_project_paywall_or_redirect` helpers + `re` import + `PAYMENT_METHOD_SOFT_CAP=10`), `app.py` (`Project` import + projects fetch + visibility filter in `/profile/<user_id>`), `templates/projects.html` (new), `templates/project_detail.html` (new), `templates/create_project.html` (new), `templates/edit_project.html` (new), `templates/profile.html` (Projects section before Posts), `templates/base.html` (avatar dropdown + mobile menu nav links), `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §3 line + §8 row + §10 risks).
- **2026-05-08 — Phase 9 complete: User uploads moved to Cloudflare R2.** Railway's ephemeral disk was wiping `/app/static/uploads/` on every deploy — uploaded photos went 404 within hours. Root cause was already in §10 (`File uploads stored on Railway disk` + the `Railway wipes /app/static/uploads` gotcha entry). **Fix shipped:** new `lib/r2.py` boto3 wrapper (4 helpers: `upload_fileobj`, `presigned_url`, `head_object`, `delete_object`; client cached at module level; `enabled()` short-circuits to False if any of the 4 R2_* env vars unset, so dev workflow keeps working without R2 creds). `save_upload()` in `app.py` now streams to R2 first; on R2 failure it falls back to local disk so the user's file isn't dropped. Same refactor in `features_routes._save_upload` and `phase3_routes._save_upload` (both blueprints had their own copy of the helper — they'd otherwise still write only to disk and bypass R2 for ~half the upload routes). New `asset_url(path)` Jinja context-processor (`app.py`) routes `uploads/...` paths through 1-hour pre-signed R2 URLs when R2 is enabled, leaves committed assets (`img/seed/...`, `css/`, `js/`) on the normal `url_for('static', ...)` flow. **DB layer unchanged** — every model field still stores `uploads/<uuid>.<ext>`; only the resolution semantics shift. **URL strategy locked: pre-signed URLs.** R2 buckets without a custom Cloudflare-DNS domain don't allow public access (Cloudflare deprecated `r2.dev` public for new accounts), so the server generates a fresh 1-hour pre-signed URL on every render. Cost is one boto3 `generate_presigned_url` call per photo per page render — ~1ms, no network. Acceptable at current scale. Future enhancement: when Bryce switches GoDaddy nameservers and `media.sovereignsociety.rich` becomes a Cloudflare-DNS-managed domain, attach it as a custom domain on the bucket → swap pre-signed URLs for permanent public URLs (URL helper change only, no DB or template change). Out of scope for this phase. **Templates audited and swapped at every site rendering a user-uploaded model field** (`User.profile_photo`, `Post.image_path`, `Story.image_path`, `Win.image_path`, `Deal.image_path`, `Reel.author.profile_photo`, `ChallengeSubmission.image_path`, `Event.cover_image`, `Space.cover_image`, `Project.cover_image`, `ProjectUpdate.image_path`) — 28 swaps across 26 templates: `base.html`, `feed.html`, `profile.html`, `space_detail.html`, `spaces.html`, `messages_inbox.html`, `messages_chat.html`, `wins.html`, `deals.html`, `deal_detail.html`, `challenge_detail.html`, `bookmarks.html`, `boardroom.html`, `event_detail.html`, `events.html`, `lessons.html`, `members.html`, `leaderboard.html`, `spotlights.html`, `search.html`, `reels.html`, `story_view.html`, `referrals.html`, `accountability.html`, `onboarding.html`, `edit_profile.html`, `projects.html`, `project_detail.html`, `edit_project.html`, `booking.html`. Static asset references (CSS, JS, seed images, logos, favicons) are unchanged — they stay on the `url_for('static')` flow because they're committed to the repo. `Reel.thumbnail_path` exists on the model but no template renders it — no swap needed. **Migration script** `scripts/null_broken_uploads.py` (one-shot, idempotent) sweeps `User.profile_photo` and nulls any value whose underlying file is gone (neither in R2 nor on local disk). Manager session runs it post-deploy via `railway run` to clean the 5 users with broken paths from Railway's ephemeral-disk era; affected users will see avatar placeholders and can re-upload via `/profile/edit`. Documented in `scripts/README.md`. **Infra:** R2 bucket `mensgroupapp` already created on Bryce's Cloudflare account (manager session 2026-05-08, same ownership pattern as Stripe and GHL). 4 R2 env vars (`R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ENDPOINT`, `R2_BUCKET`) set on Railway production by manager. boto3 round-trip already verified (write+head+presigned+delete all succeed against the live bucket). **Smoke tests** (4/4 pass): (1) `r2.enabled()` returns False with no env locally, (2) returns True under `railway run`, (3) end-to-end `upload_fileobj` + `presigned_url` + `head_object` + `delete_object` all return True against live `mensgroupapp` for `uploads/_phase9_smoke.txt`, (4) with R2 env unset, `save_upload()` writes to `static/uploads/` on disk and returns `uploads/<uuid>.<ext>` (the same key shape — DB columns are interchangeable across the two backends). **NOT in scope:** custom domain (`media.sovereignsociety.rich`) — gated on Bryce DNS switch; image processing/thumbnails/resize; migrating committed seed images (`static/img/seed/space-*.png`) — they're committed to the repo and stay on Railway disk; per-request pre-signed URL caching (acceptable cost at current traffic; revisit if `/feed` grows to 100+ photos per render). **Files touched:** `requirements.txt` (added `boto3>=1.34`), `lib/r2.py` (new), `app.py` (`save_upload` rewrite + `_inject_asset_url` context processor), `features_routes.py` (`_save_upload` rewrite), `phase3_routes.py` (`_save_upload` rewrite), 26 templates (28 swaps), `scripts/null_broken_uploads.py` (new), `scripts/README.md` (new stanza), `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §3 line + §5 R2 env rows + §8 row + §10 risks).
- **2026-05-03 — Phase 4 (post-launch UX) complete: Welcome checklist wired + manifesto page shipped.** Two distinct problems collapsed into one fix. **Problem 1:** the welcome checklist's 5 items pointed at the wrong (or missing) destinations — "Read the Manifesto" linked to `/feed` because no manifesto page existed; "RSVP to Fire to Fire" referenced an event the concurrent Phase 3 rework was wiping; "Make a post" landed users on `/feed` without surfacing the composer. **Problem 2:** the existing auto-completion helper `_auto_check_item(user_id, title_substring)` used case-insensitive `LIKE %X%` matching against the canonical title. Renaming any item silently broke the auto-check; thresholds were hardcoded into the title text ("Follow 5 Brothers" — change to 3 and the matcher misses every previously-completed row). **Fix path:** added `ChecklistItem.slug` (unique nullable column, migration `f5a6b7c8d9ea`); replaced the title matcher with `_check_item_by_slug(user_id, slug)` (kept `_auto_check_item` as a thin shim for safety per Step 3.3); locked the canonical 7 items by slug — `complete-profile`, `read-manifesto`, `first-post`, `join-space`, `follow-brothers` (relaxed 5→3), `rsvp-event` (decoupled from the wiped Fire to Fire event), `complete-lesson`. New items added: `complete-profile` and `complete-lesson` (these were never in the legacy seed). **Manifesto:** new `/manifesto` route (login_required + paywall_required); new `templates/manifesto.html` extends `base.html` (kept the post-login chrome so the mobile tab bar still works) with copy lifted *verbatim* from `templates/landing.html` lines 575-579 — the canonical "engineered reality" / "reclamation of masculine power" / "build the fire" block locked in Decisions Log on 2026-05-02. Centered 720px reading column, Cormorant Garamond serif, gold-on-black accents on `<strong>` and `<em>`, single "Back to the Society" outline button → `/feed`. The slug-based auto-check fires inside the GET handler, so just landing on the page flips the item. **Auto-check call sites wired** (slug-based, idempotent): `app.py:create_post` → `first-post`, `app.py:edit_profile` → `complete-profile` (only when both bio and profile_photo are present), `app.py:onboarding_submit` → `complete-profile` (added at the end of the handler post-step processing — picks up users who finish photo+bio via the onboarding flow before ever visiting `/profile/edit`), `app.py:join_space` → `join-space`, `app.py:toggle_follow` → `follow-brothers` (still gated on `follow_count >= 3`), `phase3_routes.event_rsvp` → `rsvp-event` (only on going/interested, not on `not_going`), `phase3_routes.complete_lesson` → `complete-lesson`. **`seed_checklist()` rewritten** with three layers of self-heal: (1) explicit pre-pass deletes any `%fire to fire%` row + its `UserChecklist` children before the alias matcher runs (so `rsvp-event` slug can't accidentally absorb a completion against a now-deleted event); (2) for each canonical item, look up by slug → exact-title → known legacy aliases (`Introduce Yourself` → first-post, `Join 2 Spaces` → join-space, `Follow 5 Brothers` → follow-brothers); on hit, backfill slug + update title/desc/link/order — preserving any existing UserChecklist completions through the rename; (3) final orphan sweep: any ChecklistItem row whose slug isn't in the canonical 7 set after the loop gets deleted along with its UserChecklist children. Net effect on a legacy DB with the prior 5 stale rows + scattered completions: converges to exactly 7 canonical rows, completions preserved across renames, dead "Fire to Fire" wiped. **Sidebar widget on `/feed`** capped at 5 items (sorted incomplete-first by `order_index`) with a "See all 7 steps" overflow link → `/welcome`. The full checklist is still passed for `checklist_pct` and `checklist_all_done` calculations — only the rendered rows are sliced. **`/feed?focus=composer`** query param read in the feed handler, passed to template as `focus_composer`, conditional `<script>` block at the bottom of `feed.html` calls `document.getElementById('postContent').focus()` + `scrollIntoView`. **Smoke tests** on the local Flask test client (DATABASE_URL pointed at `/tmp/sovereign_phase4_*.db`): fresh DB + `seed_checklist()` produces 7 items in the locked order with non-null slugs; legacy-DB simulation (5 stale rows including "Introduce Yourself" + "Fire to Fire" + an existing UserChecklist completion against "Introduce Yourself") converges to 7 canonical rows with the user's completion preserved under the renamed `first-post` slug; `/manifesto` returns 200 logged-in with the verbatim copy intact + `Back to the Society` button + auto-check fires; `/feed?focus=composer` returns 200 with the focus JS block injected; sidebar reports exactly 5 `sidebar-check-item` divs + the "See all 7 steps" overflow text; `_check_item_by_slug` is verified idempotent (calling twice yields one UserChecklist row, second call is a no-op). **What did NOT happen:** sidebar visual redesign (untouched per Step 4), no gamification points added for checklist completion, no notification on auto-complete (Phase 6 territory), no migration to align the parallel branches off `e4f5a6b7c8d9` (concurrent Phase 2/3/5/6 sessions also picked similar revids — multi-head reconciliation is a future `flask db merge` job once each phase ships). **Surprise during execution:** mid-session, my migration file `f5a6b7c8d9ea_checklist_item_slug.py` was deleted from disk by a concurrent parallel session (the Phase 6 location-visibility session created `f5a6b7c8d9e0_location_visibility.py` with a near-identical revid prefix at roughly the same wall-clock time). Re-created the migration verbatim before staging the commit. No work lost; flagged in §10 as a process risk for future multi-session days. **Files touched (this commit, exactly):** `models.py` (slug column on ChecklistItem), `migrations/versions/f5a6b7c8d9ea_checklist_item_slug.py` (new), `phase3_routes.py` (`_check_item_by_slug` + rewritten `seed_checklist` + slug-based call sites in event_rsvp/complete_lesson), `app.py` (new `/manifesto` route + slug-based call sites in create_post/edit_profile/onboarding_submit/toggle_follow/join_space + sidebar cap + focus_composer wiring in `feed`), `templates/feed.html` (sidebar template loop now uses `sidebar_checklist`, overflow link copy, focus JS at bottom), `templates/manifesto.html` (new), `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §3 line + §8 row).
- **2026-05-09 — Phase 13 complete: 6 canonical St. Pete biweekly dates locked + RSVP confirmation email.** Two coupled fixes shipped together because they both touch the events surface and the manager prompt bundled them. **(1) Recurrence vocab extended:** `RECURRENCE_RULES` in `models.py` grew `manual` as a fourth value alongside `none`/`every_thursday`/`first_and_last_thursday_monthly`. `manual` templates are NOT processed by `_generate_upcoming_occurrences` (early-return added) — child occurrences must be hand-seeded in `_seed_content`. Cleaner than expanding the auto-generator with `biweekly_thursday_anchored` because (a) the chapter cadence is short-term — 6 dates through July, not an open-ended pattern — and (b) future cohort scheduling will probably shift dates ad-hoc, so editorial control beats algorithmic generation. **(2) Canonical St. Pete dates locked by Kashi 2026-05-09:** Thu May 14, Thu May 28, Thu Jun 11, Thu Jun 25, Thu Jul 9, Thu Jul 23 — every other Thursday at 6:30 PM EST at The Temple, 155 8th Street North, Saint Petersburg, FL 33701. Phase 3's `first_and_last_thursday_monthly` rule was wrong (it generates 1st-and-last-Thursday-of-each-month, which doesn't match every-other-Thursday-anchored-to-May-14 — different dates). `_seed_content` self-heals the existing template's `recurrence_rule` from `first_and_last_thursday_monthly` → `manual`, refreshes description/time/location, then wipes child Events whose dates aren't in the canonical 6-date set (cascading delete on `EventRSVP` rows for those stale children to satisfy FK), then idempotently inserts the canonical 6 occurrences (self-heals time/location on re-run). **(3) RSVP confirmation email:** new `email_send.send_event_rsvp_confirmation(user, event, referral_url)` + `templates/emails/event_rsvp_confirmation.html` + `.txt` (gold-on-black, manifesto voice — "You're going." headline, "Bring your brothers." section with personal `/r/<code>` invite URL). Wired into `phase3_routes.event_rsvp` after the `_check_item_by_slug("rsvp-event")` call: only on `status=='going'` (skipped for `interested`/`not_going` and for `not_going`'s deletion path), calls `current_user.ensure_referral_code()` + commits to guarantee the URL has a code, builds `url_for("features.referral_landing", code=..., _external=True)`, dispatches email. Wrapped in try/except — RSVP commit always succeeds even if email fails (graceful degrade matches the rest of `email_send`). Subject format: `You're going. {event.title}, {event.date %B %d}`. **Same-phase NOT in scope:** `.ics` calendar attachments, day-of reminder email, GHL engagement-tagging on RSVP (Phase 4 territory), SMS confirmation (gated on LeadConnector phone-number purchase per memory note). **Smoke tests passed (fresh sqlite, install-gate satisfied):** 6 canonical dates seeded with correct time/location and `recurrence_rule="none"` + `is_recurrence_template=False`; idempotent re-seed (still 6 dates, same set); `_generate_upcoming_occurrences(template, weeks_ahead=8)` on the manual template returns 0 (no spurious children); `Event(recurrence_rule="not_a_real_value")` raises `ValueError("recurrence_rule must be in ['every_thursday', 'first_and_last_thursday_monthly', 'manual', 'none'], got 'not_a_real_value'")`; RSVP `going` for member with no prior `referral_code` triggered email (`ensure_referral_code()` generated `s9WdFyvimVw`, URL contained `/r/s9WdFyvimVw`); email body contained event title, `Thursday, May 14, 2026 - 6:30 PM EST`, `The Temple, 155 8th Street North, Saint Petersburg, FL 33701`; subject was `You're going. St. Petersburg Chapter Biweekly, May 14`; second member RSVP'ing `interested` did NOT fire email. **Production verification:** pending Railway redeploy after push. **Files touched:** `models.py`, `app.py`, `phase3_routes.py`, `email_send.py`, `templates/emails/event_rsvp_confirmation.html` (new), `templates/emails/event_rsvp_confirmation.txt` (new), `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §3 line + §8 row + §10 risks).
- **2026-05-09 — Phase 10 complete: form-submit UX patterns locked (post composer + profile photo).** Two narrow client-side fixes after Kashi posted the same content 3× because the post composer button didn't disable on click. **Pattern 1 — disable-on-submit + visual reset:** any feed/social form-POST should disable its submit button on first click, set busy text ("Posting..."), and clear the form's user-visible inputs (textarea, image preview, ancillary panels like the poll creator) so the user gets instant "yes, it went through" feedback while the redirect is in flight. The visual reset is wrapped in a 50ms `setTimeout` so the browser's native form serialization captures values before the wipe. The handler does NOT call `preventDefault` — vanilla form POST is preserved, and the redirect after `flash("Post published.", "success")` is what actually shows the new post. ~10 lines added to the existing `<script>` block in `templates/feed.html`. **Pattern 2 — `URL.createObjectURL` preview for file inputs:** any user-facing `<input type="file">` should swap a nearby thumbnail to a local blob URL on `change`, so the user sees the file before submitting. Implemented in `templates/edit_profile.html` (new `{% block scripts %}` block) against `.avatar.avatar-xl` — replacing the avatar's inner HTML uniformly handles both the photo-set (`<img>`) and initial-letter (`<span>`) variants. **Future audit point:** walk every `<input type="file">` in `templates/` and check whether it has a preview — items without one are UX debt (event create, win create, deal create, story create, challenge submission, project create were explicitly out of scope for this phase, as was onboarding step-1 photo which already has its own preview wiring around `templates/onboarding.html` line 152). **NOT in scope:** AJAX/fetch conversion of the post form (vanilla POST stays — the redirect is load-bearing), flash auto-dismiss timing changes, additional photo-preview wiring outside the post composer (already had one) and profile edit, server route/model/migration/env-var changes, `static/js/app.js` modifications. **Files touched:** `templates/feed.html`, `templates/edit_profile.html`, `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §8 row).
- **2026-05-09 — Phase 12 complete: 8 Spaces seeded with starter content.** Empty Spaces are a UX dead end — a member who clicks into "Sovereign Wealth" and sees nothing concludes the platform is a ghost town. Phase 12 fills every Space with 15+ posts at launch. **Strategy locked:** Off Grid is **hand-written by Kashi** (`seed_content/off_grid_posts.txt`, 18 posts, format/voice canon for the rest); the other 7 Spaces (Sovereign Wealth, Body & Iron, Awake Minds, Business Directory, The Arsenal, The Hidden Truth, Family & Legacy) are **AI-generated by `claude-sonnet-4-6`** against a system prompt anchored on the manifesto block from `templates/landing.html` lines 589-593 + 3 Off Grid posts as exemplars + per-Space theme briefs. Sonnet 4.6 chosen over Opus 4.7 for cost (this is bounded one-shot generation, not reasoning-heavy work) and for the lower 2048-token cache threshold. Adapted theme briefs for the post-Phase-6.1 renamed Spaces (`Brotherhood Ops` → `Business Directory` shifted from "running chapters" to "supporting each other's businesses"; `Red Pill Intel` → `The Hidden Truth` shifted from "calm signal vs noise" to "elite corruption / trafficking / what they don't want seen") and for the Spaces whose DB descriptions had drifted from the prompt's brief (`Awake Minds` is now "suppressed knowledge / consciousness / psychedelics," not "contrarian frameworks / reading lists"; `The Arsenal` is now "2A / preparedness / self-defense," not "tools / workshop"). **Validation locked:** generation rejects any post containing `delve into`, `dive in`, `let's explore`, `it's worth noting`, `in today's world`, `at the end of the day`, `unleash`, `journey of`, `embrace`, `harness`, `tapestry`, `realm`, `master the art`, em-dashes (`—` or `–`), exclamation points, or word counts outside 80–400; up to 2 retries per Space at the prompt level. **Distribution algorithm locked:** round-robin author across active members where active = `is_admin OR lifetime_access OR subscription_status in ('active','trialing')` AND email NOT LIKE `%@sovereign.placeholder` (excludes Phase 0B seed users); `created_at` staggered randomly across the past 21 days (`utcnow - timedelta(days=randint(0,21), hours=randint(0,23), minutes=randint(0,59))`). **Side-effect bypass locked:** direct ORM `Post(...)` inserts only, never via `app.create_post` — skips the 10-point award (`current_user.add_points(10)`), the `_check_item_by_slug("first-post")` checklist auto-check, all notifications, all GHL pushes. Otherwise running this script would dump 100+ "your first post" achievements + checklist completions + notification spam onto every active member. **Idempotency locked:** before each insert, `Post.query.filter_by(space_id=...).filter(Post.content.startswith(<title>)).first()` — re-runs skip already-inserted posts on title-prefix match. **Two-mode design locked:** `generate` writes per-Space text files under `seed_content/<slug>_posts.txt` (no DB write); `insert` reads the files and writes to DB. The split lets the manager regenerate AI content (re-running `generate`) without re-inserting and re-run `insert` idempotently. Generated files are committed to the repo so they're reviewable + re-runnable; future SoT readers can audit exactly what every member sees on the seeded Spaces. **`User.has_active_subscription` is a Python `@property`, not a column** — `User.query.filter_by(has_active_subscription=True)` would silently return everything; the script uses an explicit OR clause with the underlying columns instead. **Local httpx incompatibility:** `anthropic==0.39.0` (pinned in `requirements.txt`) requires `httpx<0.28` because the library still passes `proxies=` to httpx; my dev venv had `httpx==0.28.1` and the `Anthropic()` constructor 500s with `Client.__init__() got an unexpected keyword argument 'proxies'`. Resolved locally with `pip install 'httpx<0.28'`; production Railway container is fine because it builds from `requirements.txt` directly. Flagging in §10 because future scripts importing the live `anthropic` SDK pinned at 0.39.0 will hit the same wall.
- **2026-05-09 — Phase 12 generation models + voice anchor locked.** Sonnet 4.6 generates content via `client.messages.create()` with a single user message + cacheable system prompt (manifesto + 3 Off Grid posts + format rules, ~1.4K tokens — under the 2048-token cache minimum on Sonnet 4.6, so caching does not actually engage at this prefix size; cache_control is left in place for future prompt expansion but the cost saving is currently nil). `max_tokens=8000` per call (15 posts × ~150 words × ~1.5 tok/word = ~3.4K output, comfortable headroom). 7 calls × ~1.5K input + ~3K output × Sonnet 4.6 pricing ($3/$15 per 1M) ≈ $0.35 per full `generate --all` — cheap enough to re-run at will if the manager wants different variation.
- **2026-05-11 — Phase 14 complete: Team auto-post queue + every-other-day cron locked.** Sovereign Society Team account (prod id=11, `team@sovereignsociety.rich`, already exists from manager-session 2026-05-09→11) needs to keep dripping new starter content into each Space post-launch so the lived-in feed effect doesn't decay. Live posting is too high-touch; a queue + cadence-driven cron is the editorial-control / low-touch balance. **Cadence design locked:** every-other-day per Space, configurable via `TEAM_POST_CADENCE_DAYS` env var (default 2). Cron itself runs daily at 13:00 UTC (`0 13 * * *` = 9 AM ET / 8 AM EDT) — the per-Space check inside the command (`last team post in `post` table > now - cadence_delta?`) prevents double-firing. Daily-cron-with-internal-cadence-check beats "cron-every-other-day-globally" because (a) Railway cron schedules are global, not per-tenant; (b) the per-Space check naturally handles backfill skew where one Space's last post is 3d old and another's is 1d old; (c) a missed cron run (Railway hiccup) self-heals on the next day's run instead of waiting 48h for the next scheduled fire. **Queue/publish separation locked:** the cron does not generate or curate — it strictly drains a pre-populated `team_post_queue` table by `(queue_position ASC, created_at ASC)`. Content authoring is upstream (manager session enqueues via `scripts/team_post_enqueue.py`), removing the cron from the AI-generation / brand-voice quality loop. This is the same separation that worked for Phase 12 (`generate` vs `insert`). **Status vocab locked** (`pending|published|skipped`) — `published` is set with `published_post_id` + `published_at` populated on cron publish; `skipped` is set by the `/admin/team-queue/skip/<space_id>` POST. Rows are NEVER deleted — audit trail preserved. `@validates("status")` on the model rejects out-of-vocab values at the ORM layer. **Idempotency model locked:** the cron's "is cadence up?" check uses the most recent Team post in the `post` table (NOT the queue), so the 123 backdated Phase 12 reseed Team posts naturally respect cadence (the cron will skip any Space whose backdated post is < 2 days old). Cron is also idempotent within the cadence window — running it 3 times in a row produces the same end state as running it once (smoke-test verified). Re-running it AFTER the cadence elapses on a Space whose queue is now empty produces `skipped_empty=1` and no error. **What the cron does NOT do:** publish multiple posts per Space per run (one-and-done — leaves the queue rate-limited), skip ahead to "next priority" (strict FIFO, no priority field on the model — manager controls ordering at enqueue time by appending), fire notifications / GHL pushes / engagement tagging (Team posts are content-as-content, not a member action; engagement-tagging belongs to Phase 4), auto-populate from `seed_content/*.txt` (separate one-off ingest controlled by the manager session). **Enqueue script supports three modes** (stdin / `--file` / `--bulk-file`) to match how the manager session writes content — stdin for one-off paste, `--file` for a single AI-generated draft, `--bulk-file` for the `=====`-separated format already used by `seed_content/*.txt` from Phase 12. Bulk mode strips leading `#` header lines so the existing seed files can be replayed verbatim. **Admin tooling kept minimal** — `/admin/team-queue` shows per-Space pending count + last-published-at + next-in-queue preview + "skip next" form. No edit/delete UI; admins use `flask shell` if needed. The reasoning: a draft/review workflow would slow the manager's "write 5 posts, drop them in the queue, move on" loop; if review becomes a real ask later it's a separate phase. **Migration syntax respects Postgres-vs-SQLite gotcha** — `server_default=sa.text("'pending'")` for the status default (not bare `'pending'`), integer literal `'0'` for `queue_position`, no boolean defaults (so `sa.false()` isn't needed this migration but the convention is documented in the migration's header comment for the next one). **Files touched (3 commits):** commit 1 — `models.py` + `migrations/versions/9926b13b9552_team_post_queue_scheduled_team_posts.py`; commit 2 — `cron.py` + `app.py` (admin routes + new model import) + `scripts/team_post_enqueue.py` + `templates/admin_team_queue.html`; commit 3 — `OPERATIONS.md` + `scripts/README.md` + `INTEGRATION-SOURCE-OF-TRUTH.md` (this entry + §3 line + §5 env-var row + §8 table row + §10 risks). **Production verification:** pending — manager will set the Railway cron (`flask cron team-post-publish` at `0 13 * * *`) after this phase lands, then run one manual `railway run flask cron team-post-publish` to verify clean execution (should report `published=0` since all 8 Spaces have a fresh Team post from Phase 12).

---

## 10. Known Risks / Open Questions

- **[2026-05-02 audit gap]** The Phase 0 audit missed that `templates/pricing.html`'s founder-code UI had no server-side handler. The "Apply" button validated the code via `/validate-code` and re-styled the CTA, but clicking the CTA still routed to `/create-checkout-session` — which short-circuits to "Payment is not configured" when Stripe placeholder is set. Net effect: every visitor who entered a valid founder code hit a dead end. Resolved by Phase 0C in same-day commit (new `/signup-with-code` route + helper + UX branch). Lesson for future audits: route-grep + env-check is necessary but not sufficient — full UI flows must be smoke-tested end-to-end (click each CTA, confirm the network call hits a real handler, confirm the handler creates the expected side effect). Phase 1 audit checklist should encode this.
- **GitHub PAT exposed in git remote URL** (verified earlier) — needs rotation
- ~~**Inline GHL function in app.py** — refactor to `lib/ghl.py` in Phase 1 for testability~~ (resolved 2026-05-02 by phase-1, see §6 + Decisions Log)
- **No Celery** — background tasks use threading. Fine for MVP, watch for race conditions
- ~~**File uploads stored on Railway disk** — Railway disks are ephemeral on deploys; migrate to S3/R2 before launch~~ (resolved 2026-05-08 by Phase 9 — uploads now stream to Cloudflare R2 bucket `mensgroupapp` via `lib/r2.py`; `asset_url()` Jinja helper renders 1-hour pre-signed URLs; local-disk fallback preserved for dev workflow without R2 creds. See §9 entry.)
- **Pre-signed URL render cost is ~1ms boto3 call per photo per page render** (Phase 9, 2026-05-08). Cheap at current scale — `/feed` renders ~15-30 photos per request, total added latency well under 50ms. If `/feed` grows to 100+ photos per render (e.g., infinite scroll concatenated server-side), wrap the helper to cache pre-signed URLs in `flask.g` per-request — same key resolves once per render instead of per-`<img>`-tag. Not needed today.
- **R2 bucket `mensgroupapp` is on Bryce's Cloudflare account** (Phase 9, 2026-05-08) — same ownership pattern as Stripe and GHL. Document in `BRYCE-HANDOFF.md` so credential ownership stays mapped. R2 access key + secret are Railway-env-only; rotation requires Cloudflare dashboard access (Bryce's Google login). Bucket name was chosen for legacy/operational reasons (predates the Sovereign Society rebrand) — DO NOT rename; data is keyed by bucket name in pre-signed URL signatures and DB-stored object keys.
- **Stripe subscription cancellation handling** — needs verification (does the app actually downgrade access on cancel?)
- ~~**`User.has_active_subscription` rejects `"trialing"` status** ([models.py:120-126](models.py#L120-L126)) — current code only treats `"active"` as active. With the locked 30-day-trial model, every trial user will be paywalled out the moment they sign up. **Launch blocker.** Phase 0 fixes this — change to `self.subscription_status in ("active", "trialing")`.~~ (resolved 2026-05-02 by phase-0, commit `1116d92`)
- ~~**Public copy misrepresents the offer.** `templates/landing.html` (FAQ), `templates/pricing.html` ($100 typo + missing trial/lifetime), and `templates/legal.html` (Terms §2) all promise "$99/month for 3 months → lifetime" — a model the code does not implement and which Kashi has confirmed is NOT the actual offer. **Material contract-law exposure if signups happen against this text.** Launch blocker. Phase 0A reconciles all three files.~~ (resolved 2026-05-02 by phase-0a, commit e4aa7fc)
- **Stale support email `support@onepercentmensclub.com`** in `legal.html` (Terms §8 + Privacy §"Your Rights"). Old brand. **Q7 locked 2026-05-03:** sender domain is `sovereignsociety.rich` (NOT `.com` — verified in Resend, the only SS-branded verified domain in the account). `EMAIL_FROM=noreply@sovereignsociety.rich` set on Railway. Implications: (1) stale `support@onepercentmensclub.com` in legal.html still needs swapping to `support@sovereignsociety.rich` or removal pending Kashi's call; (2) any future SoT references to `sovereignsociety.com` (e.g., `app.sovereignsociety.com` custom-domain recommendation in §1) should be re-read as `.rich`; (3) outbound deliverability builds on the `.rich` TLD specifically; (4) for inbound (replies, support@), MX records + Workspace/forwarding setup is a separate task beyond Resend (Resend is outbound-only).
- ~~**Port 5000 hardcoded in `app.py:1709`.** Surfaced 2026-05-02 by Phase 0A executor — macOS Control Center holds port 5000, blocking local dev. Fix: change `app.run(port=5000)` to `app.run(port=int(os.getenv("PORT", 5000)))`. Trivial 1-line fix, fold into Phase 0 audit.~~ (resolved 2026-05-02 by phase-0, commit `1116d92`)
- **`legal.html` Terms §4 (Member Code of Conduct) is still placeholder text** ("Members agree to engage respectfully…"). The new §2A invokes it as a contingency for Lifetime Access continuation, so when §2A goes live, §4 needs real, enforceable conduct rules. Phase 7 (Compliance + community guidelines) work.
- **`legal.html` `Last updated: April 2026` date is stale** (actual content was updated 2026-05-02 by Phase 0A). Needs a refresh, plus a forward decision on whether to track legal-page revision history (changelog at top of file, archived versions, etc.). Cleanup before any paid signup.
- **Multiple Stripe price IDs?** — one tier today (`STRIPE_PRICE_ID`) — might need annual + monthly + founder pricing
- **GHL location separate from Stratum's** — must NOT cross-contaminate contact data between businesses
- **Repo lives in iCloud Drive (`~/Library/Mobile Documents/com~apple~CloudDocs/Desktop/anti-billionaires-app`).** iCloud syncs both the working tree AND the `.git` folder, which causes ref drift between machines (observed 2026-05-02: local `main` was 10 commits behind origin while `git pull` reported "Already up to date"). Recommend moving repo out of iCloud Drive on both machines (e.g., `~/code/anti-billionaires-app`) and re-cloning fresh from origin. Until then: every session must start with `git fetch origin && git reset --hard origin/main` (after confirming no uncommitted local work) rather than `git pull`, which can silently lie about freshness.
- **Ownership clarified 2026-05-03 (manager session, supersedes 2026-05-02 retraction).** Per Kashi: Sovereign Society is **10% Kashi / 90% Bryce**. Operational access (Railway CLI, GitHub repo) sits with Kashi but **business ownership stays with Bryce by design** — Stripe account, GHL location email (`bryce.gonzalez1986@gmail.com`), and apparel/business entity all in Bryce's name. **No transfer to Kashi is planned or desired.** The 2026-05-02 entry that retired Bryce-handoff as a "blocker" was operationally correct (Kashi can do day-to-day admin and key rotations) but understated ownership — corrected here. `BRYCE-HANDOFF.md` is retained as the credential-co-ownership reference. Implications for future managers: (1) any decision about revenue routing, account titling, or LLC paperwork is Bryce's call, not Kashi's; (2) admin promotions in the SS app itself should include Bryce; (3) ADMIN_EMAILS env var must include Bryce's email; (4) early member emails / GHL contacts will be associated with Bryce's accounts — that is intentional, not a migration failure.
- **`GEMINI_API_KEY` exposed via chat 2026-05-02.** Phase 0B executor reported that the Gemini key (Nano Banana / Google Generative AI) was pasted directly in conversation chat to invoke the image-generation skill. Once a key appears in chat it is captured in transcripts/logs we don't control — treat as compromised. **Action: rotate this key immediately** (Google AI Studio → API keys → revoke + reissue). New key goes into `.env.local` (gitignored), executor reads from env, never writes the value back to chat. Add to Bryce-handoff scope so the rotated key is centrally tracked. **Pattern enforcement going forward:** no API keys, webhook secrets, Stripe keys, or session secrets are ever pasted in chat. They live in `.env.local`, in Railway/Vercel env config, or in 1Password. Executors that need a key request it via env var only.
- **Placeholder seed content must be cleaned out OR transparently labeled before any paid launch.** Phase 0B (commit 78f8f47) seeded 8 fictional "Founding Voice" users with bios, posts, wins, deals, resources, RSVPs, and an active weekly challenge. Every placeholder user's bio ends with `— Founding Voice (pre-launch seed account)`, which is honest but visible. Their posts/wins/deals do NOT carry that label — a casual reader of `/feed` would not know they are seed content. **Decision needed before public marketing:** (a) keep the seed content as-is and treat the footer as sufficient disclosure, (b) replace the 8 placeholders with a real founder cohort (Bryce + Kashi's network) before public launch, or (c) run `python seed_placeholders.py --delete` immediately before flipping the marketing switch and operate from an empty community. Cleanup is one command; the choice is content-strategy, not technical. Tracked for Phase 1 of the customer-journey work. Dev login (placeholders only): `seed.<slug>@sovereign.placeholder` / `ChangeBeforeLaunch_2026!`.
- ~~**Production at `https://onepercentmensclub.up.railway.app` is currently NOT serving the app.**~~ **Resolved 2026-05-02 (manager session):** false alarm. The legacy URL was a stale reference in `capacitor.config.json:7` from an older deleted Railway project. The actual live URL is `https://anti-billionaires-app-production.up.railway.app` and has been serving HTTP 200 throughout. `capacitor.config.json` corrected to point at the live URL in same-day commit. The audit's curl probes were aimed at a dead URL because they trusted the stale config — this is the kind of trap that happens when no env-grounded source-of-truth is wired up. New §5 entry mandates that every future audit cross-checks live URL against `railway domain` output, not just static config files. Original blocker text retained for audit trail in git history.
- ~~**`cron.py` weekly digest references nonexistent model fields.** Surfaced 2026-05-02 by Phase 0 audit. `cron.py:64` reads `w.content` on a `Win` (Win has `title` + `description`, no `content`); `cron.py:75-77,82` filter/order by `Event.starts_at` (Event has `date` + `time`, no `starts_at`).~~ (resolved 2026-05-02 by phase-1 — `cron.py:64` now builds `f"{w.title} - {w.description}"` for the digest dict; `cron.py:75-77,82` switched to `Event.date` filter/order with `Event.time` appended to the display string. `flask cron digest` runs to completion when `SERVER_NAME` is set.)
- ~~**GHL tag taxonomy split between legacy `"ABMC"` and new `"Sovereign Society"`.**~~ (resolved 2026-05-02 by phase-1 — single 9-tag canonical taxonomy locked in `lib/ghl.STAGE_TAGS`; brand tags rejected with `ValueError`; all 5 call sites migrated; backfill script `scripts/backfill_ghl_tags.py` shipped — Kashi runs `--apply` separately once Bryce shares live location creds.)
- **`/notifications/read` and `/notifications/mark-read` are duplicate routes** (both POST, both mark-all-read; `app.py:1151` and `app.py:1185`). Not a bug — just dead weight. Pick one, delete the other, retire the duplicate at the next routine cleanup.
- ~~**`cron.py:133` admin-email lookup is buggy.**~~ (resolved 2026-05-02 by phase-1 — `cli_test_email` now does an explicit `if not admin: click.echo("No admin user found - set ADMIN_EMAILS or create one."); return` and uses `click.echo` instead of `print` so `flask cron` plays well with the CLI runner.)
- ~~**Email render fails silently in threaded sends when `_external=True` URLs are used.**~~ (resolved 2026-05-02 by phase-1 — `app.py` now sets `app.config["SERVER_NAME"] = os.environ.get("SERVER_NAME") or ("anti-billionaires-app-production.up.railway.app" if ENV=="production" else None)` and `PREFERRED_URL_SCHEME="https"`. Verified locally that with `SERVER_NAME=localhost:5099 flask cron digest`, the digest renders to completion (HTML + text variants) for 2 active users without `RuntimeError`. Verified that `SERVER_NAME=None` in dev does NOT break: `python app.py` boots, `/feed` and `/api/notifications/unread-count` return HTTP 200 when logged in. Railway env var must be set for production — manager session sets after this phase ships.)
- ~~**`/create-checkout-session` does NOT configure the 30-day Stripe trial. LAUNCH BLOCKER.**~~ **Resolved 2026-05-02 (manager session):** **false alarm.** Phase 1 executor flagged the missing `subscription_data={"trial_period_days": 30}` kwarg in `app.py:1348`, but did not have visibility into the Stripe dashboard. The 30-day trial **IS configured on the Price object itself** (`price_1TSqoGCl2bd6Dz0I44Po79vj` has `recurring.trial_period_days = 30`, verified via Stripe API in same-day manager session). When Stripe Checkout creates a subscription against a Price with a trial, the trial applies automatically — `subscription_data.trial_period_days` is only required to OVERRIDE a price's default. Subscriptions will land in `"trialing"` status as expected; the Phase 0 `has_active_subscription` fix is correctly load-bearing. No code change required. **Pattern note:** future executor sessions probing Stripe state should curl `/v1/prices/<price_id>` (read-only, restricted-key OK) before flagging a "trial not configured" finding — the Stripe-side config can move the goalposts on the local-code-only read. Original blocker text retained for audit trail in git history.
- **Recurrence generation runs on every `/events` GET** (Phase 3, 2026-05-03). The cost is one `SELECT recurrence_parent_id, date FROM event WHERE recurrence_parent_id = ?` per template (currently 2 templates) plus zero inserts when the 8-week window is full. Cheap at MVP traffic but at high traffic this becomes a measurable DB cost. **If `/events` traffic exceeds ~10 req/sec sustained, move generation to a Railway cron** (e.g. `flask cron generate-events` running every hour) and drop the inline call from the route. The generator function `_generate_upcoming_occurrences` is already idempotent and isolated in `app.py` — moving it is a 5-line refactor.
- **Migration chain has multiple parallel heads as of 2026-05-03.** Phases 3, 4, 5, 6 all branched from `e4f5a6b7c8d9` and shipped independent migrations: `b1e2f5a6c7d8` (Phase 3 events), `f5a6b7c8d9e0` (Phase 6 location_visibility), `f5a6b7c8d9ea` (Phase 4 checklist_item_slug), `f5a6b7c8d9ec` (Phase 5 assessment_models). When all four phases are committed and pushed, alembic will see 4 heads and `flask db upgrade` will refuse with "Multiple head revisions are present." **A merge-migration is required before the next deploy** (e.g. `flask db revision -m "merge phases 3-6" --depends-on=b1e2f5a6c7d8 --depends-on=f5a6b7c8d9e0 --depends-on=f5a6b7c8d9ea --depends-on=f5a6b7c8d9ec` or `flask db merge heads -m "phase 3-6 merge"`). Manager-level coordination needed; not in any single phase's scope. Until merged, Railway redeploys will fail at the `flask db upgrade` step. **Two of the four migrations originally collided on revision id `f5a6b7c8d9e0`** (Phase 3 events + Phase 6 location_visibility); Phase 3 renamed to `b1e2f5a6c7d8` to clear that collision, but the merge-migration is still required.
- **Nominatim polite-use rate cap (~1 req/sec, ~10K req/day).** Phase 6's `/find/search` geocodes fresh queries through Nominatim with no key; `lib/geocoding.py` caches results in-process via `lru_cache(1024)` so repeat lookups are free, but bursty traffic against fresh queries (e.g. a marketing wave that lands many members on `/find` typing distinct cities at once) could trip the polite-use threshold. No proactive throttle is wired (would block a request behind another in-flight request inside a single worker, which fights the threading model). **Mitigation when traffic justifies:** swap `geocode_city` to Mapbox Geocoding ($0.50 / 1k requests, generous free tier) or Google Places (more expensive, better coverage). Both keep the same `(lat, lng) | None` return contract — drop-in. Track real volume after first marketing push; flip if `requests.get` against Nominatim ever errors out for sustained windows. Cache TTL is also unbounded (process lifetime); on Railway redeploy the cache is empty — that's fine, just be aware.
- **Find Brothers leaks member name + city to any logged-in member.** Phase 6's `/find/search` and `/find/nearby` return `name`, `city`, `country`, `profile_photo`, and distance for every matching `User.location_visibility != 'hidden'` (search) or `== 'proximity_visible'` (nearby) row. This is by design — the feature exists to let brothers find each other — but it does mean any signed-in member can enumerate all non-hidden member names by sweeping cities. If a member wants their NAME hidden but city visible, today the only option is `location_visibility='hidden'` (which also hides them from city search, which they may not want). **Decision needed before public launch:** is name-only-hide a use case worth a 4th tier (`city_no_name`?) or is the existing 3-tier model sufficient and we communicate the tradeoff in the visibility radio explainer copy? Defer to Kashi/Bryce. Until decided, the existing radio copy ("don't show my location anywhere" for `hidden`) is honest about the binary.
- **Concurrent-phase migration multi-head debt.** As of 2026-05-03 there are 4 active heads in `migrations/versions/` all parented at `e4f5a6b7c8d9`: `b1e2f5a6c7d8_events_type_chapter_recurrence` (Phase 3 — committed), `f5a6b7c8d9ea_checklist_item_slug` (Phase 4 — pending), `f5a6b7c8d9ec_assessment_models` (Phase 5 — pending), `f5a6b7c8d9e0_location_visibility` (Phase 6 — this commit). `flask db upgrade` will fail with "Multiple head revisions" until they're merged. **Resolution path:** once all 4 phases land on `main`, a single `flask db merge -m "merge phase 3+4+5+6 heads" <revid1> <revid2> <revid3> <revid4>` produces a no-op merge migration that reunifies the chain. Railway deploy command (`flask db upgrade`) will then succeed against the new tip. NOT this phase's responsibility per Step 4 — but flagging here so the manager session that ships the last of 3/4/5/6 generates the merge migration as part of that final commit.
- **iCloud Drive sync glitches during multi-session days.** Observed during the Phase 6 build session: `models.py` was reverted by an iCloud sync mid-session (column added → file overwritten with old version → re-added) and an earlier-written migration file `f5a6b7c8d9e0_location_visibility.py` momentarily disappeared from disk before being re-created. Working tree is at `/Users/kenneth/anti-billionaires-app/` but a stale parallel copy exists at `/Users/kenneth/Desktop/anti-billionaires-app/` (the iCloud-Drive path), confirming the `§10` 2026-05-02 entry that the repo lives in iCloud Drive on at least one machine. **Mitigation while this persists:** verify file contents after every Write/Edit in long-running sessions; commit early (per phase, per surface) rather than batching; treat `git status` as authoritative when in doubt; consider re-cloning to `~/code/anti-billionaires-app` (out of iCloud) on the affected machine.
- **Apparel line in scope but not yet scoped.** Per Kashi 2026-05-02: launch a Sovereign Society apparel line (T-shirts, hats, hoodies, "earned merch" pendant for Lifetime-Qualified members) connected to the membership. **Recommended MVP path:** print-on-demand via Printful or Printify, separate Shopify storefront at `shop.sovereignsociety.com` ($39/mo), 4–5 SKUs to launch (logo crewneck tee, manifesto back-print tee, embroidered cap, hoodie, and small-batch manufactured "earned" pendant). Tier integration: 20% member discount via Stripe coupon, Founding-100 numbered tee for first 100 members, Lifetime-Qualified members receive the pendant free. Total upfront: ~$1k–2k for POD launch; manufactured hero piece comes later when revenue justifies. Estimated 5–20 orders week 1, scale tied to Society membership growth. Decisions still needed: which Stripe account (existing membership Stripe or separate Shopify account), apparel designer (freelance vs Nano Banana for patterns + human for typography), launch timing relative to Society public launch. Phase prompt for apparel-line setup TBD — gated on Society launch first OR can run in parallel if Kashi prioritizes.
- **`AssessmentResponse.answers_json` and `pillar_scores_json` are stored as JSON strings, not normalized rows** (Phase 5, 2026-05-03). Tradeoff: keeps the schema cheap and lets question text/pillar count change without migrations, but breaks per-pillar SQL queries — e.g., "find members scoring >= 4 on Wealth" requires either parsing JSON in the query (Postgres `->>` cast on Railway is fine; SQLite has `json_extract` but slower) or migrating to a normalized `(response_id, pillar_slug, score)` row table later. Acceptable for MVP because no consumer of pillar scores exists yet (matching/recommendations explicitly out of scope). Revisit when AI Wingman or directory features want to filter members by pillar strength.
- **Multi-head migration state across parallel build phases** (2026-05-03). Three untracked migration files exist locally from concurrent Phase 2/4/6 sessions and all branch off the same down-rev `e4f5a6b7c8d9` (Phase 2 events: `f5a6b7c8d9e0_events_type_chapter_recurrence.py`, Phase 4 checklist: `f5a6b7c8d9ea_checklist_item_slug.py`, Phase 6 location: `f5a6b7c8d9e0_location_visibility.py`). Two of them collide on revision ID `f5a6b7c8d9e0`. This Phase 5 migration uses unique revision `f5a6b7c8d9ec` to avoid colliding with mine, but once all four phases land on `main`, alembic will see four heads and (where two share the same revision ID) fail to upgrade. **Action:** when the second build phase ships, an alembic merge migration (`flask db merge -m "merge build phases" <head1> <head2> ...`) is required, and the two `f5a6b7c8d9e0`-colliding migrations must be renamed to unique IDs first. Tracked here so the next phase-shipper doesn't get blindsided.
- **Project visibility tiers introduce a new gating surface** (Phase 7, 2026-05-03). The `_visible_projects(query, user)` helper in `features_routes.py` is THE single read-path filter: feed, detail, interest toggle, AND `app.py:profile` all funnel through equivalent visibility logic. **Risk:** if a future change to a project read path bypasses this helper (e.g., a new admin inspection route, an export script, an API endpoint, a notifications digest), a non-Lifetime member could see a `brotherhood_only` project — privacy bug. **Mitigation discipline:** any new read of `Project` rows must either (a) call `_visible_projects` to scope the query, or (b) explicitly document in a comment why the bypass is correct (e.g., admin-scoped or self-view). Grep `Project.query` on every PR that touches projects; flag any uses that don't go through the helper. Same discipline applies to `ProjectUpdate` and `ProjectInterest` reads — they're scoped through their parent project's visibility transitively, so any direct query of those tables must also respect the project tier.
- **Payment-method directory leaks creator's wallet/handle to any logged-in member who can see the project** (Phase 7, 2026-05-03). Per Step 2.6, this is by design — payment methods are a directory, the whole point is that members can send the creator funds off-platform. But it does mean: (a) `members_only` payment methods are visible to the entire active-subscriber base; (b) `brotherhood_only` payment methods are visible to all Lifetime-Qualified members. If a member doxxes a wallet address by re-publishing it elsewhere, that's outside SS's control. **Decision needed before public launch:** add an explicit "your wallet/handle is visible to other members" warning at payment-method creation time (currently absent — the form just takes the handle), or trust members to understand that adding a payment method to a public-ish project page makes the address public. Defer to Kashi/Bryce. Also: there is currently no rate-limit on payment-method creation per project (only a soft cap of 10/project) — if a malicious creator wanted to spray fake addresses to test how members react, they'd hit the 10-cap quickly but could keep creating projects to repeat the trick. Accept for MVP; revisit if abuse appears.
- **Manual chapter-event scheduling requires code edit + redeploy** (Phase 13, 2026-05-09). The St. Pete chapter biweekly's 6 canonical dates (May 14 → Jul 23) are hand-coded in `_seed_content` because `recurrence_rule='manual'` skips the auto-generator. Adding July's next cohort, swapping a date, or seeding a new chapter (e.g. Austin) all require editing the `canonical_st_pete_dates` set in `app.py`, committing, and waiting for Railway to redeploy. Acceptable for the current 6-event horizon — Kashi reseeds infrequently. **Mitigation when chapter cadence stabilizes:** build a small admin UI under `/events/admin` to add/edit chapter occurrences from the browser (POST creates an `Event` row with `recurrence_parent_id` set to the manual template; admin-only). Until then: any cohort-management ask requires Claude Code session + redeploy.
- **RSVP confirmation email exposes member's referral code in plaintext URL** (Phase 13, 2026-05-09). The email sent on RSVP-going contains a fully-formed `/r/<code>` invite URL the member is encouraged to forward. If forwarded, the recipient could pre-load the referrer code on signup. **This is intended behavior — that's the entire point of referral codes** — flagging here only because future GDPR-style privacy reviews might surface it. The code is also visible to the member in the email-render and in `/referrals` already, so the email exposure is not a new leakage surface.
- **Project-payment-method shape regex is heuristic, not authoritative** (Phase 7, 2026-05-03). The 10 `method_type` regexes in `models.py:PROJECT_PAYMENT_METHOD_TYPES` are anchored full-match shape checks — they reject obviously-wrong inputs (`"bogus"`, `"0xfoo"`, an empty string) but DO NOT verify the address resolves on-chain or that a Cash App tag is registered. A creator could publish `0x` followed by 40 random hex chars and the form would accept it. Members must verify with the creator before sending — that's what the load-bearing disclaimer banner says. **Mitigation when traffic justifies:** swap the BTC regex for a proper bech32 + base58 checksum check, swap the Solana regex for base58 length+alphabet checking, and add an optional "verify with creator" CTA that messages the creator on the spot (uses the existing DM feature). Not MVP. Note also that the `custom_link` regex `^https?://[^\s]+$` is intentionally permissive — it's the fallback for "anything else" and gets the same disclaimer banner; if abuse happens here (e.g., phishing links posted as payment methods), gate `custom_link` behind admin approval.
- **AI-generated starter content is attributed to real members, not labeled as seed** (Phase 12, 2026-05-09). 7 of the 8 Spaces seeded by `scripts/seed_space_content.py insert --all` are AI-written by `claude-sonnet-4-6` against the manifesto voice anchor. Posts are inserted directly via ORM under real active members' user IDs (round-robin attribution), with `created_at` staggered across the past 21 days — making them structurally indistinguishable from posts those members actually wrote. **Disclosure risk if discovered:** a member who reads through their own profile and sees a post they didn't write will (a) be confused, (b) plausibly accuse SS of impersonating them. **Mitigation as shipped:** quality bar enforced by banned-phrase + em-dash + word-count filters at generation time; manager reviews each Space's content file (`seed_content/<slug>_posts.txt`) before running insert; Off Grid (Kashi-written) is left untouched. **Mitigation if disclosure becomes a problem:** (a) flip attribution to a single dedicated `seed@sovereign.placeholder` user with a "Founding Voice (community seed)" badge — costs the "lived-in feed" effect because every starter post then visibly comes from one account; (b) add a footer to every seeded post (`— pre-launch starter content`) — costs the "feels organic" effect; (c) reach out to the active members ahead of time, get explicit consent for attribution, and treat it as ghostwriting (the most ethical path but requires real-time coordination). Not yet decided. **Re-running `insert --all` is idempotent** — once seeded, members will not get duplicate posts even if the script runs again.
- **`anthropic==0.39.0` requires `httpx<0.28`** (Phase 12, 2026-05-09). Pinned in `requirements.txt`. The 0.39.0 SDK passes `proxies=` to httpx's `Client.__init__`, which httpx 0.28+ removed. Symptom: `Anthropic()` constructor raises `TypeError: Client.__init__() got an unexpected keyword argument 'proxies'`. Production Railway container builds from `requirements.txt` with both pins satisfied, so prod is fine — but a fresh dev venv installed without `requirements.txt` (e.g. `pip install anthropic`) pulls latest httpx and breaks. **Fix when it bites:** `pip install 'httpx<0.28'` in the dev venv. **Permanent fix when bandwidth allows:** bump `anthropic` to a 0.50+ release that supports modern httpx; not done in Phase 12 because it would also touch any other call sites that import `anthropic` (e.g. `features_routes.wingman` if it ever moves off the placeholder reply) and is not blocking. Future scripts in this repo importing `anthropic` will need to know about this constraint.
- **TEAM_POST_CADENCE_DAYS mid-deploy change affects in-flight cadence** (Phase 14, 2026-05-11). The cron checks `now - cadence_delta > last_team_post.created_at` against the CURRENT env-var value, not the value at the time the previous post was published. Shortening cadence (e.g. 2 → 1) will trigger publishes for Spaces whose last post was yesterday — acceptable, intentional. Lengthening cadence (e.g. 2 → 7) makes the queue go quiet until enough time elapses on the longest-since-last-published Space. Manager should communicate any change before flipping the env on Railway so a "did the cron break?" panic doesn't follow.
- **Team-post cron failures are silent unless Railway alerting is configured** (Phase 14, 2026-05-11). The command logs to stdout (`[TEAM-POST] cadence=2d published=X skipped_recent=Y skipped_empty=Z`) but a Railway cron-job failure (container OOM, DB connection blip, alembic-migration-not-yet-applied) won't notify anyone unless Railway's per-job alert is wired into Slack/email. **Mitigation:** manager should add `/admin/team-queue` to the weekly ops-review checklist — if `pending` counts are growing while `last published` stays stuck, the cron isn't firing. **Permanent fix when bandwidth allows:** add a `last_cron_run_at` row to a singleton config table and surface "last cron run: 3 days ago (stale)" on the admin page in red.

---

## 11. Update Rules for Future Sessions

1. Read this entire file before doing any work.
2. After completing a task, update §8 (Phase Status) — change ⬜ to ✅, add notes.
3. If you make a decision, append to §9 with date and reason.
4. If you discover a new risk, append to §10.
5. New env vars / pipelines / tags / workflows go into §5, §6 in the same commit.
6. Commit message format: `phase-N: <what changed>` (lowercase). Example: `phase-1: lift ghl client to lib/ghl.py`.
7. Never delete entries from the Decisions Log.

---

## 12. Reference: Customer Journey Playbook

This project will get its own community-flavored playbook in Phase 6. Until then, reference the Stratum playbook structure at `~/stratum-therapeutics/CUSTOMER-JOURNEY-PLAYBOOK.md` and the reusable template at `~/claude-code-playbook/template-customer-journey.md` — same methodology, different content.

---

## 13. Phase 0 Audit Report — 2026-05-02

This appendix captures the read-only discovery output from Phase 0. The two trivial bug fixes shipped in commit `1116d92`. This audit ships in its own commit so code-changes and doc-changes have separate blame.

### 13.1 Routes Inventory (complete)

**`app.py` — 54 routes**

| Method | Path | Handler |
|--------|------|---------|
| GET | `/` | `index` |
| POST | `/api/devices/register` | `register_device` |
| POST | `/api/devices/unregister` | `unregister_device` |
| GET, POST | `/preferences/digest` | `toggle_digest` |
| GET | `/terms` | `terms` |
| GET | `/privacy` | `privacy` |
| GET | `/onboarding` | `onboarding` |
| POST | `/onboarding` | `onboarding_submit` |
| GET | `/feed` | `feed` |
| POST | `/feed` | `create_post` |
| POST | `/like/<int:post_id>` | `toggle_like` |
| POST | `/comment/<int:post_id>` | `add_comment` |
| DELETE | `/post/<int:post_id>` | `delete_post` |
| GET, POST | `/login` | `login` |
| GET, POST | `/signup` | `signup` |
| GET | `/logout` | `logout` |
| GET, POST | `/forgot-password` | `forgot_password` |
| GET, POST | `/reset-password/<token>` | `reset_password` |
| GET | `/verify-email/<token>` | `verify_email` |
| POST | `/resend-verification` | `resend_verification` |
| GET | `/profile/<int:user_id>` | `profile` |
| GET, POST | `/profile/edit` | `edit_profile` |
| GET | `/members` | `members` |
| GET | `/leaderboard` | `leaderboard` |
| POST | `/follow/<int:user_id>` | `toggle_follow` |
| GET | `/spaces` | `spaces` |
| GET | `/space/<int:space_id>` | `space_detail` |
| GET, POST | `/space/create` | `create_space` |
| POST | `/space/<int:space_id>/join` | `join_space` |
| POST | `/space/<int:space_id>/leave` | `leave_space` |
| POST | `/space/<int:space_id>/post` | `create_space_post` |
| GET | `/notifications` | `notifications` |
| POST | `/notifications/read` | `mark_notifications_read` |
| GET | `/api/notifications/unread-count` | `api_unread_count` |
| GET | `/api/notifications/recent` | `api_recent_notifications` |
| POST | `/notifications/mark-read` | `mark_read` *(duplicate of `/notifications/read`)* |
| POST | `/poll/vote/<int:option_id>` | `vote_poll` |
| GET | `/pricing` | `pricing` |
| POST | `/validate-code` | `validate_code` |
| POST | `/create-checkout-session` | `create_checkout_session` |
| GET, POST | `/subscription/success` | `subscription_success` |
| POST | `/webhook/stripe` | `stripe_webhook` *(CSRF-exempt)* |
| POST | `/billing-portal` | `billing_portal` |
| GET | `/admin` | `admin_panel` |
| GET | `/admin/member/<int:user_id>` | `admin_member_detail` |
| POST | `/admin/toggle-admin/<int:user_id>` | `toggle_admin` |
| POST | `/admin/toggle-subscription/<int:user_id>` | `toggle_subscription` |
| POST | `/admin/grant-lifetime/<int:user_id>` | `admin_grant_lifetime` |
| POST | `/admin/revoke-lifetime/<int:user_id>` | `admin_revoke_lifetime` |
| POST | `/admin/refund-last/<int:user_id>` | `admin_refund_last` |
| POST | `/admin/comp-month/<int:user_id>` | `admin_comp_month` |
| GET | `/learn` | `learn` *(redirect → phase3.lessons)* |

**`phase3_routes.py` — 9 routes (blueprint `phase3`)**

| Method | Path | Handler |
|--------|------|---------|
| GET | `/events` | `events` |
| GET | `/events/<int:event_id>` | `event_detail` |
| GET, POST | `/events/create` | `create_event` *(admin-only)* |
| POST | `/events/<int:event_id>/rsvp` | `event_rsvp` |
| GET | `/lessons` | `lessons` |
| GET | `/lessons/<int:course_id>/<int:lesson_id>` | `lesson_detail` |
| POST | `/lessons/<int:course_id>/<int:lesson_id>/complete` | `complete_lesson` |
| GET | `/welcome` | `welcome` *(checklist)* |
| POST | `/welcome/check/<int:item_id>` | `check_item` |

**`features_routes.py` — 42 routes (blueprint `features`)**

| Method | Path | Handler |
|--------|------|---------|
| GET | `/messages` | `inbox` |
| GET | `/messages/new/<int:user_id>` | `new_conversation` |
| GET | `/messages/<int:convo_id>` | `chat` |
| POST | `/messages/<int:convo_id>/send` | `send_message` |
| GET | `/messages/<int:convo_id>/poll` | `poll_messages` |
| GET | `/api/messages/unread-count` | `api_unread_messages` |
| POST | `/stories/create` | `create_story` |
| GET | `/stories/<int:story_id>` | `view_story` |
| GET | `/api/stories` | `api_stories` |
| GET | `/wins` | `wins` |
| POST | `/wins/create` | `create_win` |
| POST | `/wins/<int:win_id>/react` | `react_win` |
| GET | `/deals` | `deals` |
| GET, POST | `/deals/create` | `create_deal` |
| GET | `/deals/<int:deal_id>` | `deal_detail` |
| POST | `/deals/<int:deal_id>/interest` | `deal_interest` |
| GET | `/challenges` | `challenges` |
| GET, POST | `/challenges/create` | `create_challenge` *(admin-only)* |
| GET | `/challenges/<int:ch_id>` | `challenge_detail` |
| POST | `/challenges/<int:ch_id>/submit` | `submit_challenge` |
| POST | `/challenges/submission/<int:sub_id>/vote` | `vote_submission` |
| GET | `/resources` | `resources` |
| GET, POST | `/resources/create` | `create_resource` |
| POST | `/resources/<int:res_id>/upvote` | `upvote_resource` |
| GET | `/referrals` | `referrals` |
| GET | `/r/<code>` | `referral_landing` *(public)* |
| GET | `/accountability` | `accountability` |
| POST | `/accountability/pair/<int:user_id>` | `create_pair` |
| POST | `/accountability/goals/create` | `create_goal` |
| POST | `/accountability/goals/<int:goal_id>/checkin` | `goal_checkin` |
| POST | `/accountability/goals/<int:goal_id>/complete` | `complete_goal` |
| GET | `/bookmarks` | `bookmarks` |
| POST | `/bookmark/<int:post_id>` | `toggle_bookmark` |
| GET | `/badges` | `badges_page` |
| GET | `/reels` | `reels` |
| GET, POST | `/reels/create` | `create_reel` |
| GET | `/space/<int:space_id>/chat` | `space_chat` |
| POST | `/space/<int:space_id>/chat/send` | `send_space_chat` |
| GET | `/space/<int:space_id>/chat/poll` | `poll_space_chat` |
| GET | `/wingman` | `wingman` |
| POST | `/wingman/send` | `wingman_send` |
| GET | `/map` | `member_map` |
| POST | `/profile/location` | `update_location` |
| GET | `/book/<int:user_id>` | `booking_page` |
| POST | `/book/<int:user_id>/create` | `create_booking` |
| GET | `/bookings` | `my_bookings` |
| POST | `/bookings/<int:booking_id>/confirm` | `confirm_booking` |
| POST | `/bookings/<int:booking_id>/cancel` | `cancel_booking` |
| GET | `/boardroom` | `boardroom` *(Platinum or Level 9+)* |
| GET | `/spotlights` | `spotlights` |
| GET | `/activity` | `activity_feed` |
| GET | `/search` | `search` |

(Counts: app.py 54 + phase3 9 + features 42 = 105 unique route definitions.)

### 13.2 Stripe State

- **Mode (local):** `STRIPE_SECRET_KEY` is UNSET in this dev shell → `app.py:82` falls back to literal `"sk_test_placeholder"`. `app.py:1242` short-circuits checkout when the key is placeholder. Cannot determine production mode without Railway dashboard access.
- **`STRIPE_PRICE_ID`:** UNSET locally; default `"price_placeholder"`. Validity in production needs manual verification by Kashi (Stripe CLI not installed locally; no `stripe prices retrieve` available).
- **Webhook route:** `POST /webhook/stripe` (`app.py:1361`), CSRF-exempt, signature-verified via `stripe.Webhook.construct_event` against `STRIPE_WEBHOOK_SECRET`. Idempotent via `StripeEvent` table on `stripe_event_id`.
- **Webhook events handled:**
  - `checkout.session.completed` → `_handle_checkout_completed` (backfills `stripe_customer_id` if user already exists by email)
  - `customer.subscription.updated` → `_handle_subscription_updated` (refreshes `subscription_status` + `current_period_end`; skips if user is `lifetime_access`)
  - `customer.subscription.deleted` → `_handle_subscription_deleted` (sets status `canceled`, **calls `ghl_upsert_contact(...tags=["Churned", "ABMC"])`**)
  - `invoice.payment_succeeded` → `_handle_payment_succeeded` (increments `payments_made_count`; at 6, increments referrer's `qualified_referrals_count`; at 3, flips referrer to `lifetime_access=True`, cancels referrer's Stripe sub, calls `ghl_upsert_contact(...tags=["Lifetime", "ABMC"])` and `send_lifetime_unlocked` email)
  - `invoice.payment_failed` → `_handle_payment_failed` (sets status `past_due`, sends `send_payment_failed` email)
- **Webhook → GHL:** YES on `subscription.deleted` (Churned tag) and `payment_succeeded → lifetime branch` (Lifetime tag). NO on the other 3 events. (Phase 2 widens this.)

### 13.3 GHL State

- `ghl_upsert_contact()` shape and call sites: see updated §6.
- `GHL_API_KEY`: UNSET locally. `GHL_LOCATION_ID`: UNSET locally.
- No live ping attempted (env unset → no auth).

### 13.4 Deploy State

- **Builder:** Railway with NIXPACKS (`railway.json`), Python provider (`nixpacks.toml`).
- **Procfile:** `web: gunicorn app:app -w 4 --threads 2 --worker-class gthread --timeout 60`.
- **Start command (production):** `flask db upgrade && gunicorn app:app -w 4 --threads 2 --worker-class gthread --timeout 60` (from `railway.json`). Migrations run automatically before gunicorn starts. Restart policy: `ON_FAILURE` × 10.
- **Live URL:** `https://onepercentmensclub.up.railway.app` (declared in `capacitor.config.json:7`). `curl -I` returns HTTP 404 with `x-railway-fallback: true` on `/`, `/pricing`, `/healthz` → **production is currently down or service is unbound**. See §10.
- **Last code deploy:** Most recent commit on `origin/main` is `02d87f1` (sot: log gemini api key exposure + rotation pattern). Railway auto-deploys from main, so it should have run, but the live URL is not serving — disconnect between repo and runtime.
- **Migrations:** 7 alembic revisions in `migrations/versions/`:
  1. `43be3a8d6b40_initial_schema_baseline`
  2. `88a0045f8905_auth_hardening_lifetime_access_fields`
  3. `9c1f2a3e4d5b_stripe_event_idempotency_log`
  4. `abcd1234e5f6_engagement_email_throttling`
  5. `c2d3e4f5a6b7_perf_indexes` (23 hot-path indexes)
  6. `d3e4f5a6b7c8_device_tokens`
  7. `e4f5a6b7c8d9_referral_qualification_fields`
- **`_seed_content()` trigger:** `app.py:399-405` runs at module import time inside `with app.app_context()`, wrapped in try/except. Gunicorn forks 4 workers; this will run 4 times, but `_seed_content()` is idempotent (existence checks before insert). Side effect: brief startup log noise. Fine for MVP.
- **`seed_checklist()` and `seed_badges()`** also run at startup (`app.py:271-278`) — also idempotent.

### 13.5 Email Infrastructure

- **Service:** Resend (`resend==2.0.0` in `requirements.txt`; `email_send.py:18-29`).
- **Sender:** `EMAIL_FROM` defaults to `onboarding@resend.dev` (Resend's shared sandbox domain — works for testing but will land in spam at scale and signals "not a real product"). `EMAIL_FROM_NAME` defaults to `Sovereign Society`.
- **Production sender domain (TBD):** No custom sender domain configured today. Needs DKIM/SPF/DMARC at the registrar of whatever domain Sovereign Society lands on — likely `sovereignsociety.com` per Phase 0A intent. Also tied to the stale `support@onepercentmensclub.com` cleanup in §10.
- **Fallback:** when `RESEND_API_KEY` is unset, `_send_now` prints to stdout (console stub) — dev-friendly, won't crash.
- **Templates (`templates/emails/`):** `_layout.html`, `welcome_verify`, `password_reset`, `payment_succeeded`, `payment_failed`, `referral_qualified`, `lifetime_unlocked`, `weekly_digest` — each with `.html` + `.txt` pair (15 files total + 1 layout).
- **Typed senders (`email_send.py`):** `send_welcome_verify`, `send_password_reset`, `send_payment_succeeded`, `send_referral_progress`, `send_payment_failed`, `send_lifetime_unlocked`, `send_weekly_digest`, plus the generic `send_email`.
- **Engagement throttling (`cron.py`):** `notify_dm_throttled` enforces 1 DM-email per recipient per hour via `User.last_engagement_email_at`. Weekly digest enforces 6-day idempotency via `User.last_digest_sent_at`. Both honor `email_digest_opt_out`. Unsubscribe link → `/preferences/digest`.

### 13.6 Database State

- **`DATABASE_URL`:** UNSET locally → `app.py:49` falls back to `sqlite:///abmc.db`. `app.py:50-51` rewrites `postgres://` → `postgresql://` (Railway gives the bare prefix; SQLAlchemy 1.4+ wants the explicit driver).
- **Local instance dir:** `/instance/` does not exist on this machine — fresh checkout, no SQLite has been created here. Not a problem (will create on first `python app.py` run).
- **Engine pool config (Postgres only):** `pool_pre_ping=True`, `pool_size=5`, `max_overflow=10`, `pool_recycle=1800` — sane defaults for Railway's connection limits.
- **Migration count:** 7 (see §13.4).
- **Indexes:** the `c2d3e4f5a6b7_perf_indexes` migration creates 23 indexes covering hot read paths: post (user_id, space_id, created_at), comment (post_id), like (post_id, user_id), notification (user_id, is_read), conversation (user1, user2), message (conversation_id, is_read), win (created_at), space_membership (user_id, space_id), user (subscription_status, lifetime_access), ai_chat (user_id, created_at), activity (user_id), bookmark (user_id), follow (follower_id, followed_id). Also explicit indexes on `User.email_verify_token` and `User.password_reset_token` (in the model definition).
- **Indexes worth flagging for production load (not changing now):** no index on `User.email` (it's the `unique=True` column so it gets one implicitly — fine), no index on `User.referred_by` (referral lookups will table-scan once `User.count()` grows past a few thousand — Phase 1 should add).

### 13.7 Other surprises / launch-blocker candidates

- **Production is currently 404.** Already in §10 — repeated here because it surfaced during this audit and is the single most important thing to resolve before any Phase 1 work. Until the Railway service is bound + healthy, every other phase is blocked from production verification.
- **`cron.py:64,75-77,82` field bugs.** Already in §10. Weekly digest will crash on first run.
- **`cron.py:133` admin-email lookup bug.** Already in §10. Test-email CLI will crash if no admin user exists.
- **GHL tag taxonomy split.** Already in §10. Workflows that filter on `"ABMC"` will miss paid signups; workflows filtering on `"Sovereign Society"` will miss everything else.
- **`/notifications/read` and `/notifications/mark-read` duplicate routes.** Already in §10.
- **Capacitor bundle ID + server URL still legacy.** `appId: com.onepercentmensclub.app`, `server.url: https://onepercentmensclub.up.railway.app`, `ios.scheme: OnePercentMC`. Per CLAUDE.md these are infrastructure-tied and intentionally NOT renamed (Apple/Google review process pain). Document only — no action.
- **PWA manifest is minimal but valid.** `static/manifest.json` declares Sovereign Society branding, dark theme color, two icon sizes. Acceptable for v1.
- **No TODO/FIXME/XXX markers** anywhere in `app.py`, `phase3_routes.py`, `features_routes.py`, `email_send.py`, `models.py`, or `cron.py`. Either the codebase is genuinely clean of inline cruft, or such markers were never used as a convention.
- **No `lib/` directory yet.** GHL client lifting (Phase 1 scope) will create it.
- **No tests currently run as part of the deploy.** `pytest` and `pytest-flask` are in `requirements.txt` but no `tests/` directory exists, no CI workflow file (`.github/workflows/`) was found in the read-mandated paths. Deploy validation is purely manual today. Acceptable for MVP, but flag once paid signups happen.
